% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_rating.R
\name{sdt_rating_logmu}
\alias{sdt_rating_logmu}
\alias{sdt_dpsdt_logmu}
\alias{sdt_metad_logmu}
\title{Rating SDT category log-probability (multinomial logit)}
\usage{
sdt_rating_logmu(
  cat,
  K,
  dist,
  thresh,
  d,
  criterion,
  spacing,
  sdratio,
  stimulus,
  ...
)

sdt_dpsdt_logmu(
  cat,
  K,
  dist,
  thresh,
  d,
  criterion,
  spacing,
  sdratio,
  Ro,
  Rn,
  stimulus,
  ...
)

sdt_metad_logmu(
  cat,
  K,
  dist,
  thresh,
  d,
  criterion,
  spacing,
  sdratio,
  logmratio,
  stimulus,
  ...
)
}
\arguments{
\item{cat}{Integer rating category index.}

\item{K}{Integer number of rating categories.}

\item{dist}{Integer noise-distribution id (see the \code{.sdt_dists} registry).}

\item{thresh}{Integer threshold-parameterization id.}

\item{d, criterion, spacing, sdratio}{Model parameters (draws-by-observation
matrices supplied by brms). \code{d} is the balanced sensitivity index d_a;
\code{spacing} is \code{0} for threshold types without it.}

\item{stimulus}{Stimulus covariate (0 = noise, 1 = signal).}

\item{...}{Threshold \code{delta} parameters, when the threshold type uses them.}

\item{Ro, Rn}{Linear-scale recollection parameters for the \code{dpsdt} version;
\code{inv_logit(Ro)}/\code{inv_logit(Rn)} are the recollection probabilities.}

\item{logmratio}{Log M-ratio for the \code{metad} version; meta-d' is recovered
as \code{exp(logmratio) * d}, on the same d_a scale as \code{d}.}
}
\value{
\code{log(p_cat)}, matching the shape of \code{d}.
}
\description{
R companion to the Stan \code{sdt_rating_logmu} function. It returns
\code{log(p_k)} for rating category \code{cat}, which the \code{\link[=sdt_rating]{sdt_rating()}} multinomial
formula uses as the category logit (so \code{softmax} recovers the SDT category
probabilities). \code{brms} evaluates the non-linear formula in R for
\code{posterior_predict()} and \code{posterior_epred()}, so this function must be on
the search path; it is exported for that reason and is not called directly.
}
\keyword{internal}
