% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_mafc.R
\name{sdt_mafc}
\alias{sdt_mafc}
\title{m-Alternative Forced Choice Signal Detection Theory Model}
\usage{
sdt_mafc(
  response,
  n_trials,
  m,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A single string naming the column with counts of correct
responses.}

\item{n_trials}{The name of the variable containing the total number of
trials per cell.}

\item{m}{Integer. Number of alternatives (must be >= 2).}

\item{dist}{Character. Currently only "normal" is supported.}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
m-Alternative Forced Choice Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} m-Alternative Forced Choice
\item \strong{Name:} Signal Detection Theory (m-AFC)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
DeCarlo, L. T. (2012). On a signal detection approach to m-alternative forced choice with bias, with maximum likelihood and Bayesian approaches to estimation. JMASM, 11(1), 257-282.
}
\item \strong{Version:} mafc
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{mu}: Internal parameter (fixed to 0)
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item mu = identity; dprime = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
}
}

Models accuracy in m-AFC tasks. Only the \code{dprime} parameter is estimated
(no criterion). For 2-AFC, uses the closed-form \verb{Phi(d'/sqrt(2))}. For
m >= 3, uses 20-point Gauss-Hermite quadrature.
}
\examples{
\dontrun{
dat <- rsdt_mafc(n_per_cell = 200, n_subjects = 20,
                 dprime = 1.5, m = 4)

model <- sdt_mafc(
  response = "n_correct",
  n_trials = "n_trials",
  m = 4
)

fit <- bmm(
  formula = bmf(dprime ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
}
}
\references{
DeCarlo, L. T. (2012). On a signal detection approach to m-alternative
forced choice with bias, with maximum likelihood and Bayesian approaches
to estimation. \emph{Journal of Mathematical and Statistical Psychology},
\emph{11}(1), 257--282.
}
\keyword{bmmodel}
