% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_metad.R
\name{sdt_metad}
\alias{sdt_metad}
\title{Meta-d' Signal Detection Theory Model}
\usage{
sdt_metad(
  response,
  stimulus,
  n_ratings = NULL,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  threshold_type = c("parsimonious", "equidistant", "log_distance", "log_ratio",
    "softmax"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A character vector of K column names containing response
counts per rating category, ordered from "definitely noise" to
"definitely signal". Requires K > 2.}

\item{stimulus}{The name of the variable coding the stimulus type.
Must be coded as 0 (noise/new) and 1 (signal/old).}

\item{n_ratings}{Integer. Number of response categories. Defaults to
\code{length(response)}. Must be > 2.}

\item{dist}{Character. The noise distribution to use:
\itemize{
\item "normal" (default): Gaussian SDT
\item "logistic": Logistic SDT
\item "gumbel_min": Extreme-value (Gumbel minimum) SDT
\item "gumbel_max": Gumbel maximum SDT
}}

\item{threshold_type}{Character. Threshold parameterization (see
\code{\link{sdt_rating}} for details).}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Meta-d' Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Meta-d' Signal Detection Theory Model
\item \strong{Citation:}
\itemize{
\item Maniscalco, B., & Lau, H. (2012). A signal detection theoretic approach for estimating metacognitive sensitivity from confidence ratings. Consciousness and Cognition, 21(1), 422-430.
}
\item \strong{Version:} metad
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
\item \code{spacing}: Threshold spacing: controls distance between adjacent thresholds (exp(spacing) ensures positive spacing)
\item \code{metad}: Metacognitive sensitivity: type-2 d' controlling confidence threshold placement. If metad = dprime, metacognition is ideal.
\item \code{sdratio}: SD ratio: log ratio of signal to noise standard deviations (exp(sdratio) ensures positivity, 0 = equal variance)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{sdratio} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item dprime = identity; criterion = identity; spacing = identity; metad = identity; sdratio = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{metad}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{sdratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
}
}

The meta-d' model adds a \code{metad} parameter for metacognitive sensitivity.
If metad equals dprime, metacognition is ideal. Requires confidence
rating data.

By default, the model assumes equal variance (sdratio fixed to 0). To
estimate unequal variance, add \code{sdratio ~ 1} to the formula.
}
\examples{
\dontrun{
dat <- rsdt_metad(n_per_cell = 200, n_subjects = 20,
                  dprime = 1.5, criterion = 0, metad = 1.0,
                  n_ratings = 4, spacing = 0.5)

model <- sdt_metad(
  response = c("r1", "r2", "r3", "r4"),
  stimulus = "stimulus"
)

fit <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1, metad ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
}
}
\references{
Maniscalco, B., & Lau, H. (2012). A signal detection theoretic approach
for estimating metacognitive sensitivity from confidence ratings.
\emph{Consciousness and Cognition}, \emph{21}(1), 422--430.
\doi{10.1016/j.concog.2011.09.021}
}
\keyword{bmmodel}
