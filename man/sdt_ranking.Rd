% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_ranking.R
\name{sdt_ranking}
\alias{sdt_ranking}
\title{Ranking Signal Detection Theory Model}
\usage{
sdt_ranking(
  response,
  rank,
  m,
  dist = c("gumbel_min", "normal"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A single string naming the column with rank frequency
counts.}

\item{rank}{The name of the variable coding the rank position (1 = most
likely target, m = least likely).}

\item{m}{Either a single integer >= 2 giving the number of ranked items
(constant across all rows), or a single string naming a data column that
gives the number of ranked items per row.}

\item{dist}{Character. The noise distribution:
\itemize{
\item "gumbel_min" (default): Closed-form Gumbel-min ranking
\item "normal": Gaussian ranking (supports unequal variance via
sdratio)
}}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Ranking Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Ranking Task
\item \strong{Name:} Signal Detection Theory (Ranking)
\item \strong{Citation:}
\itemize{
\item Meyer-Grant, C. G., Kellen, D., Harding, S. M., & Singmann, H. (2025). Extreme-value signal detection theory for recognition memory: The parametric road not taken. PsyArXiv. https://doi.org/10.31234/osf.io/qhrfj
}
\item \strong{Version:} ranking
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{mu}: Internal parameter (fixed to 0)
\item \code{dprime}: Sensitivity: ranking discrimination parameter
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item mu = identity; dprime = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
}
}

Models rank ordering of m items by perceived strength. Only \code{dprime}
is estimated (no criterion). Supports \code{dist = "gumbel_min"} (closed-form
via lgamma ratios) and \code{dist = "normal"} (numerical integration).

For Gaussian ranking (\code{dist = "normal"}), sdratio is fixed to 0 by
default. Add \code{sdratio ~ 1} to the formula for unequal-variance ranking.

The set size \code{m} may be a constant or the name of a data column. Supply a
column to fit trials with different set sizes in a single model.
}
\examples{
\dontrun{
dat <- rsdt_ranking(n_per_cell = 200, n_subjects = 20,
                    dprime = 1.5, m = 4)

model <- sdt_ranking(
  response = "observed",
  rank = "rank",
  m = 4
)

fit <- bmm(
  formula = bmf(dprime ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
}
}
\references{
Meyer-Grant, C. G., Kellen, D., Harding, S. M., & Singmann, H. (2025).
\emph{Extreme-value signal detection theory for recognition memory: The
parametric road not taken}. PsyArXiv preprint.
\doi{10.31234/osf.io/qhrfj}
}
\keyword{bmmodel}
