% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{mixture3p_cd_dist}
\alias{mixture3p_cd_dist}
\alias{dmixture3p_cd}
\alias{rmixture3p_cd}
\title{Distribution functions for the mixture3p change detection model}
\usage{
dmixture3p_cd(
  response,
  probe,
  nt_features,
  lure_idx,
  kappa = 5,
  thetat = log(6),
  thetant = log(3),
  beta = 0,
  mu = 0,
  log = FALSE
)

rmixture3p_cd(
  n,
  probe,
  nt_features,
  lure_idx,
  kappa = 5,
  thetat = log(6),
  thetant = log(3),
  beta = 0,
  mu = 0
)
}
\arguments{
\item{response}{Binary vector (0 = "same", 1 = "change")}

\item{probe}{Probe value in radians (centered relative to target)}

\item{nt_features}{Numeric vector of non-target feature values (radians,
centered relative to target)}

\item{lure_idx}{Integer vector indicating active non-targets (1) or
inactive (0)}

\item{kappa}{Concentration parameter of the von Mises distribution}

\item{thetat}{Target log-weight for softmax normalization (guessing is the
reference category with log-weight 0). A value of \code{log(6)} together with
\code{thetant = log(3)} gives P(target)=0.6, P(non-target)=0.3, P(guess)=0.1.}

\item{thetant}{Non-target log-weight for softmax normalization (same scale
as \code{thetat}).}

\item{beta}{Decision criterion. Default 0.}

\item{log}{Logical; if \code{TRUE}, return log probability.}

\item{n}{Number of observations to generate}
}
\description{
Density and random generation for the three-parameter mixture
model applied to the change detection task.
}
\keyword{distribution}
