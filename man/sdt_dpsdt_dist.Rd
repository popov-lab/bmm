% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_dpsdt_dist}
\alias{sdt_dpsdt_dist}
\alias{dsdt_dpsdt}
\alias{rsdt_dpsdt}
\title{Distribution functions for dual-process SDT (DPSDT)}
\usage{
dsdt_dpsdt(
  counts,
  stimulus,
  dprime,
  thresholds,
  Ro,
  Rn,
  sdratio = 1,
  dist = "normal",
  log = FALSE
)

rsdt_dpsdt(
  n_per_cell,
  n_subjects,
  dprime,
  criterion,
  Ro,
  Rn,
  sdratio = 1,
  dist = "normal",
  n_ratings = NULL,
  spacing = NULL,
  deltas = NULL,
  threshold_type = "parsimonious"
)
}
\arguments{
\item{counts}{Integer vector of length K. Response counts per rating
category for a single observation, ordered from "definitely noise" (1) to
"definitely signal" (K).}

\item{stimulus}{Integer (0 or 1). Stimulus type: 0 = noise, 1 = signal.}

\item{dprime}{Numeric. Sensitivity parameter.}

\item{thresholds}{Numeric vector of length K-1. Ordered decision thresholds.}

\item{Ro}{Numeric in \verb{[0, 1]}. Recollection probability for old (signal)
items.}

\item{Rn}{Numeric in \verb{[0, 1]}. Recollection (recall-to-reject) probability
for new (noise) items.}

\item{sdratio}{Numeric. Ratio of signal to noise standard deviations
(default 1, i.e., equal variance).}

\item{dist}{Character. Noise distribution: "normal" (default), "logistic",
"gumbel_min", or "gumbel_max".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per stimulus type per subject.}

\item{n_subjects}{Integer. Number of subjects.}

\item{criterion}{Numeric. Central decision threshold (response bias).}

\item{n_ratings}{Integer. Number of response categories (must be >= 3).}

\item{spacing}{Numeric. Threshold spacing parameter for "equidistant",
"parsimonious", and "softmax" threshold types.}

\item{deltas}{Numeric vector. Parameters for "log_distance", "log_ratio", or
"softmax" threshold types.}

\item{threshold_type}{Character. Threshold parameterization: "parsimonious"
(default), "equidistant", "log_distance", "log_ratio", or "softmax".}
}
\value{
\code{dsdt_dpsdt} returns the (log-)density (multinomial probability).
\code{rsdt_dpsdt} returns a data frame with columns \code{id}, \code{stimulus},
\code{r1}, ..., \code{rK}, and \code{nTrials}.
}
\description{
Density and random generation for the dual-process signal
detection model (Yonelinas, 1994). Extends rating SDT with recollection
probabilities \code{Ro} (old items recollected as old) and \code{Rn} (new items
recall-rejected) that add mass to the most-confident rating category. These
are the simulation counterparts of the \code{dpsdt} version of \code{\link[=sdt_rating]{sdt_rating()}};
here \code{Ro}/\code{Rn} are supplied directly as probabilities in \verb{[0, 1]}.
}
\examples{
# Density for a single observation (K=4) with recollection of old items
dsdt_dpsdt(counts = c(2, 8, 20, 70), stimulus = 1,
           dprime = 1.5, thresholds = c(-0.5, 0.0, 0.5), Ro = 0.3, Rn = 0)
# Generate DPSDT rating data (K=4)
dat <- rsdt_dpsdt(n_per_cell = 100, n_subjects = 10, dprime = 1.5,
                  criterion = 0, Ro = 0.3, Rn = 0.1, n_ratings = 4,
                  spacing = 0.5)
head(dat)
}
\references{
Yonelinas, A. P. (1994). Receiver-operating characteristics in recognition
memory: Evidence for a dual-process model. \emph{Journal of Experimental
Psychology: Learning, Memory, and Cognition}, \emph{20}(6), 1341--1354.
\doi{10.1037/0278-7393.20.6.1341}
}
\keyword{distribution}
