% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_rating.R
\name{sdt_rating}
\alias{sdt_rating}
\title{Confidence Rating Signal Detection Theory Model}
\usage{
sdt_rating(
  response,
  stimulus,
  n_ratings = NULL,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  threshold_type = c("parsimonious", "equidistant", "log_distance", "log_ratio",
    "softmax"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A character vector of K column names containing response
counts per rating category, ordered from "definitely noise" to
"definitely signal".}

\item{stimulus}{The name of the variable coding the stimulus type.
Must be coded as 0 (noise/new) and 1 (signal/old).}

\item{n_ratings}{Integer. Number of response categories. When \code{response}
is a character vector, defaults to its length. Must be > 2.}

\item{dist}{The noise distribution assumed for the latent evidence variable.
One of:
\itemize{
\item "normal" (default): Gaussian SDT
\item "logistic": Logistic SDT
\item "gumbel_min": Extreme-value (Gumbel minimum) SDT
\item "gumbel_max": Gumbel maximum SDT
}}

\item{threshold_type}{Character. Threshold parameterization:
\itemize{
\item "parsimonious" (default): 2 parameters (criterion + spacing).
Thresholds follow logit-spaced canonical positions (Selker et al.,
2019).
\item "equidistant": 2 parameters (criterion + spacing). Thresholds
are equally spaced around criterion.
\item "log_distance": K-2 parameters. Each threshold distance is
exp(delta), guaranteeing ordering.
\item "log_ratio": K-2 parameters. Threshold distances as ratios
(Paulewicz & Blaut, 2020).
\item "softmax": K-2 parameters. A shared spacing parameter controls
the average interval size, while softmax-transformed delta parameters
allocate interval widths smoothly across the scale.
}}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Confidence Rating Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Signal Detection Theory (Confidence Rating)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
}
\item \strong{Version:} rating
\item \strong{Requirements:}

Provide pre-aggregated data with the following columns:
}
\itemize{
\item Response counts: one column per rating category (K columns)
\item Stimulus type (stimulus): 0 = noise, 1 = signal
Categories should be ordered: 1 = 'definitely noise' to K = 'definitely signal'
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
\item \code{spacing}: Threshold spacing: controls distance between adjacent thresholds (exp(spacing) ensures positive spacing)
\item \code{sdratio}: Log SD ratio: the log of the signal-to-noise standard deviation ratio, so exp(sdratio) is the ratio itself and 0 means equal variance
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{sdratio} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item dprime = identity; criterion = identity; spacing = identity; sdratio = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{sdratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
}
}

By default, the model assumes equal variance (sdratio fixed to 0). To
estimate unequal variance, add \code{sdratio ~ 1} (or \code{sdratio ~ predictors})
to the formula.
}
\examples{
\dontrun{
# EV-SDT rating model
dat <- rsdt_rating(n_per_cell = 200, n_subjects = 20,
                   dprime = 1.5, criterion = 0, n_ratings = 4,
                   spacing = 0.5)

model <- sdt_rating(
  response = c("r1", "r2", "r3", "r4"),
  stimulus = "stimulus"
)

fit <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)

# UV-SDT: add sdratio to the formula
fit_uv <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1, sdratio ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
}
}
\references{
Green, D. M., & Swets, J. A. (1966). \emph{Signal detection theory and
psychophysics}. Wiley.

Selker, R., van den Bergh, D., Criss, A. H., & Wagenmakers, E.-J. (2019).
Parsimonious estimation of signal detection models from confidence ratings.
\emph{Behavior Research Methods}, \emph{51}(5), 1953--1967.
\doi{10.3758/s13428-019-01231-3}

Paulewicz, B., & Blaut, A. (2020). The bhsdtr package: A general-purpose
method of Bayesian inference for signal detection theory models.
\emph{Behavior Research Methods}, \emph{52}, 2122--2141.
\doi{10.3758/s13428-020-01370-y}
}
\keyword{bmmodel}
