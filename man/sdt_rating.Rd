% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_rating.R
\name{sdt_rating}
\alias{sdt_rating}
\title{Confidence Rating Signal Detection Theory Model}
\usage{
sdt_rating(
  response,
  stimulus,
  n_ratings = NULL,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  threshold_type = c("parsimonious", "equidistant", "log_distance", "log_ratio",
    "softmax"),
  version = c("standard", "dpsdt", "metad"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A character vector of K column names containing response
counts per rating category, ordered from "definitely noise" to
"definitely signal".}

\item{stimulus}{The name of the variable coding the stimulus type.
Must be coded as 0 (noise/new) and 1 (signal/old).}

\item{n_ratings}{Integer. Number of response categories. When \code{response}
is a character vector, defaults to its length. Must be > 2.}

\item{dist}{The noise distribution assumed for the latent evidence variable.
One of:
\itemize{
\item "normal" (default): Gaussian SDT
\item "logistic": Logistic SDT
\item "gumbel_min": Extreme-value (Gumbel minimum) SDT
\item "gumbel_max": Gumbel maximum SDT
}}

\item{threshold_type}{Character. Threshold parameterization:
\itemize{
\item "parsimonious" (default): 2 parameters (criterion + spacing).
Thresholds follow logit-spaced canonical positions (Selker et al.,
2019).
\item "equidistant": 2 parameters (criterion + spacing). Thresholds
are equally spaced around criterion.
\item "log_distance": K-2 parameters. Each threshold distance is
exp(delta), guaranteeing ordering.
\item "log_ratio": K-2 parameters. Threshold distances as ratios
(Paulewicz & Blaut, 2020).
\item "softmax": K-2 parameters. A shared spacing parameter controls
the average interval size, while softmax-transformed delta parameters
allocate interval widths smoothly across the scale.
}}

\item{version}{Character. The latent-process version of the rating model.
One of \code{"standard"} (default, a single familiarity SDT process),
\code{"dpsdt"} (dual-process SDT with recollection parameters \code{Ro}/\code{Rn}), or
\code{"metad"} (meta-d' with a metacognitive efficiency parameter \code{logmratio},
the log M-ratio). All three use the same confidence-rating response
interface. See Details.}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Confidence Rating Signal Detection Theory Model
}
\details{
Three versions share the confidence-rating response interface,
selected with \code{version}:
\subsection{Version: \code{standard} (default)}{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Signal Detection Theory (Confidence Rating)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
}
\item \strong{Version:} standard
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
\item \code{spacing}: Threshold spacing: controls distance between adjacent thresholds (exp(spacing) ensures positive spacing)
\item \code{sdratio}: Log SD ratio: the log of the signal-to-noise standard deviation ratio, so exp(sdratio) is the ratio itself and 0 means equal variance
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{sdratio} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item dprime = identity; criterion = identity; spacing = identity; sdratio = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{sdratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
}
}
}

\subsection{Version: \code{dpsdt}}{

Dual-process SDT (Yonelinas, 1994): a familiarity SDT process plus a
recollection threshold process. \code{Ro} is recollection of old items (loads the
most-confident "signal" category) and \code{Rn} is recall-to-reject of new items
(loads the most-confident "noise" category); \code{inv_logit(Ro)}/\code{inv_logit(Rn)}
are the recollection probabilities. Both are fixed off by default
(recovering \code{standard}); add \code{Ro ~ 1} for the one-sided model and
\verb{Ro ~ 1, Rn ~ 1} for the two-sided model.
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Signal Detection Theory (Confidence Rating)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
}
\item \strong{Version:} dpsdt
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
\item \code{spacing}: Threshold spacing: controls distance between adjacent thresholds (exp(spacing) ensures positive spacing)
\item \code{sdratio}: Log SD ratio: the log of the signal-to-noise standard deviation ratio, so exp(sdratio) is the ratio itself and 0 means equal variance
\item \code{Ro}: Recollection of old items: probability inv_logit(Ro) that an old item is recollected as old, loading the most-confident 'signal' category
\item \code{Rn}: Recollection of new items: probability inv_logit(Rn) that a new item is recall-rejected, loading the most-confident 'noise' category
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{sdratio} = 0
\item \code{Ro} = -100
\item \code{Rn} = -100
}
\item \strong{Default parameter links:}
\itemize{
\item dprime = identity; criterion = identity; spacing = identity; sdratio = identity; Ro = identity; Rn = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{sdratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{Ro}:
\itemize{
\item \code{main}: normal(0, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{Rn}:
\itemize{
\item \code{main}: normal(0, 1)
\item \code{effects}: normal(0, 0.5)
}
}
}
}

\subsection{Version: \code{metad}}{

Meta-d' (Maniscalco & Lau, 2012): a type-2 metacognitive sensitivity governs
confidence-threshold placement, with the total "old"/"new" response rates
held to what type-1 \code{dprime} implies. Rather than estimating meta-d'
directly, the model estimates \code{logmratio}, the log M-ratio
\eqn{\log(\mathrm{meta\text{-}d'}/d')}, and recovers meta-d' as
\code{exp(logmratio) * dprime}. The M-ratio is the field-standard measure of
metacognitive efficiency (Maniscalco & Lau, 2014; Fleming, 2017): estimating
it on the log scale keeps meta-d' positive, regularizes it toward \code{dprime},
and anchors the ideal point (meta-d' = \code{dprime}, perfect metacognition) at
\code{logmratio = 0}, which recovers \code{standard}. Extract the M-ratio posterior
with \code{\link[=mratio]{mratio()}}.
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Signal Detection Theory (Confidence Rating)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
}
\item \strong{Version:} metad
\item \strong{Requirements:}
\item \strong{Parameters:}
\itemize{
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
\item \code{spacing}: Threshold spacing: controls distance between adjacent thresholds (exp(spacing) ensures positive spacing)
\item \code{sdratio}: Log SD ratio: the log of the signal-to-noise standard deviation ratio, so exp(sdratio) is the ratio itself and 0 means equal variance
\item \code{logmratio}: Log M-ratio, log(meta-d'/d'): metacognitive efficiency. 0 is ideal metacognition (meta-d' = d'), negative is inefficiency, positive is hyper-efficiency. meta-d' is recovered as exp(logmratio) * dprime
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{sdratio} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item dprime = identity; criterion = identity; spacing = identity; sdratio = identity; logmratio = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{sdratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{logmratio}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
}
}

By default, the model assumes equal variance (sdratio fixed to 0). To
estimate unequal variance, add \code{sdratio ~ 1} (or \code{sdratio ~ predictors})
to the formula.
}
}
\examples{
\dontrun{
# EV-SDT rating model
dat <- rsdt_rating(n_per_cell = 200, n_subjects = 20,
                   dprime = 1.5, criterion = 0, n_ratings = 4,
                   spacing = 0.5)

model <- sdt_rating(
  response = c("r1", "r2", "r3", "r4"),
  stimulus = "stimulus"
)

fit <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)

# UV-SDT: add sdratio to the formula
fit_uv <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1, sdratio ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)

# Dual-process SDT: free recollection of old items (one-sided) or both
# old and new items (two-sided) via the formula
model_dp <- sdt_rating(c("r1", "r2", "r3", "r4"), "stimulus", version = "dpsdt")
fit_dp <- bmm(bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1, Ro ~ 1, Rn ~ 1),
              data = dat, model = model_dp, backend = "cmdstanr")

# Meta-d': estimate metacognitive efficiency (log M-ratio)
model_md <- sdt_rating(c("r1", "r2", "r3", "r4"), "stimulus", version = "metad")
fit_md <- bmm(bmf(dprime ~ 1, criterion ~ 1, spacing ~ 1, logmratio ~ 1),
              data = dat, model = model_md, backend = "cmdstanr")
mratio(fit_md) # posterior M-ratio (meta-d'/d')
}
}
\references{
Yonelinas, A. P. (1994). Receiver-operating characteristics in recognition
memory: Evidence for a dual-process model. \emph{Journal of Experimental
Psychology: Learning, Memory, and Cognition}, \emph{20}(6), 1341--1354.

Maniscalco, B., & Lau, H. (2012). A signal detection theoretic approach for
estimating metacognitive sensitivity from confidence ratings.
\emph{Consciousness and Cognition}, \emph{21}(1), 422--430.

Maniscalco, B., & Lau, H. (2014). Signal detection theory analysis of type 1
and type 2 data: meta-d', response-specific meta-d', and the unequal
variance SDT model. In S. M. Fleming & C. D. Frith (Eds.), \emph{The
cognitive neuroscience of metacognition} (pp. 25--66). Springer.

Fleming, S. M. (2017). HMeta-d: hierarchical Bayesian estimation of
metacognitive efficiency from confidence ratings. \emph{Neuroscience of
Consciousness}, \emph{2017}(1), nix007. \doi{10.1093/nc/nix007}

Green, D. M., & Swets, J. A. (1966). \emph{Signal detection theory and
psychophysics}. Wiley.

Selker, R., van den Bergh, D., Criss, A. H., & Wagenmakers, E.-J. (2019).
Parsimonious estimation of signal detection models from confidence ratings.
\emph{Behavior Research Methods}, \emph{51}(5), 1953--1967.
\doi{10.3758/s13428-019-01231-3}

Paulewicz, B., & Blaut, A. (2020). The bhsdtr package: A general-purpose
method of Bayesian inference for signal detection theory models.
\emph{Behavior Research Methods}, \emph{52}, 2122--2141.
\doi{10.3758/s13428-020-01370-y}
}
\keyword{bmmodel}
