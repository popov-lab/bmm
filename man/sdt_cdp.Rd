% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_cdp.R
\name{sdt_cdp}
\alias{sdt_cdp}
\title{Continuous Dual-Process Signal Detection Theory Model}
\usage{
sdt_cdp(
  new_response,
  old_know,
  old_remember,
  old_guess = NULL,
  stimulus,
  n_ratings = NULL,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  threshold_type = c("parsimonious", "equidistant"),
  links = NULL,
  ...
)
}
\arguments{
\item{new_response}{A character vector of column names containing response
counts for "new" confidence levels, ordered from "definitely new" to
"guess new". Can have a different length than \code{old_know}/\code{old_remember}.}

\item{old_know}{A character vector of column names containing "old-know"
response counts, ordered from "guess old" to "definitely old".}

\item{old_remember}{A character vector of column names containing
"old-remember" response counts, same ordering as \code{old_know}.}

\item{old_guess}{Optional character vector of column names for "old-guess"
response counts. If provided, enables the 3-way R/K/G model with the
\code{kcrit} parameter. Must have the same length as \code{old_know}.}

\item{stimulus}{The name of the variable coding the stimulus type.
Must be coded as 0 (noise/new) and 1 (signal/old).}

\item{n_ratings}{Integer. Total number of confidence levels. Defaults to
\code{length(new_response) + length(old_know)}. Must be >= 3.}

\item{dist}{Character. The noise distribution to use:
\itemize{
\item "normal" (default): Gaussian SDT
\item "logistic": Logistic SDT
\item "gumbel_min": Extreme-value (Gumbel minimum) SDT
\item "gumbel_max": Gumbel maximum SDT
}}

\item{threshold_type}{Character. Threshold parameterization:
"parsimonious" (default) or "equidistant".}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Continuous Dual-Process Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Recognition Memory
\item \strong{Task:} Old/New Recognition with Remember/Know Judgments
\item \strong{Name:} Continuous Dual-Process Signal Detection Theory (CDP)
\item \strong{Citation:}
\itemize{
\item Wixted, J. T., & Mickes, L. (2010). A continuous dual-process model of remember/know judgments. Psychological Review, 117(4), 1025-1054.
}
\item \strong{Version:} cdp
\item \strong{Requirements:}

Provide pre-aggregated data with the following columns:
}
\itemize{
\item 'New' response counts: 0 columns
\item 'Old-Know' counts: 0 columns
\item 'Old-Remember' counts: 0 columns
\item Stimulus type (stimulus): 0 = noise, 1 = signal
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{dprimef}: Familiarity sensitivity: target mean on familiarity axis
\item \code{dprimer}: Recollection sensitivity: target mean on recollection axis
\item \code{criterion}: Response bias: central confidence criterion on F+R axis
\item \code{spacing}: Threshold spacing: controls distance between adjacent confidence thresholds (exp(spacing) ensures positive spacing)
\item \code{rcrit}: Remember criterion: threshold on recollection axis
\item \code{sigmar}: SD ratio: log ratio of recollection target to noise SD (exp(sigmar) ensures positivity, 0 = equal variance)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{sigmar} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item dprimef = identity; dprimer = identity; criterion = identity; spacing = identity; rcrit = identity; sigmar = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprimef}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{dprimer}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
\item \code{spacing}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{rcrit}:
\itemize{
\item \code{main}: normal(0, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{sigmar}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
}
}

The CDP model (Wixted & Mickes, 2010) assumes two independent continuous
signal-detection dimensions: Familiarity (F) and Recollection (R). Old/new
confidence ratings are based on the aggregated signal F + R. Remember/Know
judgments are based on R exceeding a criterion on the recollection axis.

The number of "new" and "old" confidence levels need not be equal. For
example, a 6-point scale with 1 "new" level and 5 "old" levels (each with
R/K split) is supported — as used in Rotello et al. (2005).

Optionally, a Know/Guess split uses F exceeding a second criterion on the
familiarity axis. To enable the 3-way R/K/G model, provide \code{old_guess}
columns and add \code{kcrit ~ 1} to the formula.

By default, the model assumes equal variance for recollection (sigmar
fixed to 0). To estimate unequal variance, add \code{sigmar ~ 1} to the formula.

When only confidence rating data is available (no R/K split), the CDP model
reduces to unequal-variance SDT on the aggregated F + R axis. Use
\code{\link[=sdt_rating]{sdt_rating()}} for that case.
}
\examples{
\dontrun{
# Symmetric 6-point scale (3 new + 3 old)
model <- sdt_cdp(
  new_response = c("n1", "n2", "n3"),
  old_know = c("k4", "k5", "k6"),
  old_remember = c("r4", "r5", "r6"),
  stimulus = "stimulus"
)

# Asymmetric scale (1 new + 5 old, as in Rotello et al. 2005)
model <- sdt_cdp(
  new_response = c("new"),
  old_know = c("k2", "k3", "k4", "k5", "k6"),
  old_remember = c("r2", "r3", "r4", "r5", "r6"),
  stimulus = "stimulus"
)
}
}
\references{
Wixted, J. T., & Mickes, L. (2010). A continuous dual-process model of
remember/know judgments. \emph{Psychological Review}, \emph{117}(4),
1025--1054. \doi{10.1037/a0020874}
}
\keyword{bmmodel}
