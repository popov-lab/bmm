% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lnr.R
\name{lnr}
\alias{lnr}
\title{Log-Normal Race Model}
\usage{
lnr(
  rt,
  response,
  n_alternatives = NULL,
  version = c("simple", "custom"),
  num_alternatives = NULL,
  links = NULL,
  ...
)
}
\arguments{
\item{rt}{The name of the variable in the dataset containing the response
times. Response times should be coded in seconds (not milliseconds).}

\item{response}{The name of the variable in the dataset containing the
response/choice. For the \code{"simple"} version, responses should be
integer-coded: 1 = correct response, 2 through K = error responses.
Factor and character-digit responses are accepted and converted
automatically. For the \code{"custom"} version, responses should be character
or factor labels matching the accumulator names in the formula. Category
names must not use reserved internal parameter names such as \code{"mu"},
\code{"ndt"}, or \code{"s"}.}

\item{n_alternatives}{An integer specifying the total number of response
alternatives (K >= 2). Required for \code{version = "simple"}. Not used for
\code{version = "custom"} (inferred from the formula).}

\item{version}{A character string specifying which version of the LNR model
to use. Options are:
\itemize{
\item \code{"simple"} (default): Two meanlog parameters — \code{correct} for the
correct accumulator (response = 1) and \code{error} for all error
accumulators. The sdlog parameter \code{s} is shared and fixed by default
(s = 1). This covers the common case where interest is in the speed
of correct vs. error processing.
\item \code{"custom"}: Per-category meanlog parameters. Response categories
are defined by the formula LHS names (e.g., \verb{correct ~ 1, other ~ 1, npl ~ 1}). The response column must contain character labels matching
these names. Category names must not be \code{"mu"}, \code{"ndt"}, \code{"s"},
Stan reserved words, or names ending in a number. Supports
per-category \code{num_alternatives}.
}}

\item{num_alternatives}{For \code{version = "custom"} only. A named vector
specifying the number of racing accumulators per response category.
Can be a named integer vector of positive counts for constant numbers of
accumulators (e.g., \code{c(correct = 1, other = 3, npl = 5)}) or a named
character vector of column names whose values are positive integers for
trial-varying counts (e.g., \code{c(correct = "n_corr", other = "n_other", npl = "n_npl")}). If omitted, defaults to 1 accumulator per category.}

\item{links}{A named list of link functions for the model parameters.
For \code{"simple"}: parameters are \code{correct}, \code{error}, \code{ndt}, and \code{s}.
Default links are "identity" for \code{correct} and \code{error}, "log" for
\code{ndt} and \code{s}. For \code{"custom"}: category parameters default to "identity".}

\item{...}{Additional arguments passed internally (for testing purposes).}
}
\value{
An object of class \code{bmmodel}
}
\description{
Log-Normal Race Model
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks (multi-alternative)
\item \strong{Name:} Log-Normal Race Model
\item \strong{Citation:}
\itemize{
\item Rouder, J. N., Province, J. M., Morey, R. D., Gomez, P., & Heathcote, A. (2015).
The Lognormal Race: A Cognitive-Process Model of Choice and Latency with Desirable
Psychometric Properties. Psychometrika, 80(2), 491-513. https://doi.org/10.1007/s11336-013-9396-3
}
\item \strong{Version:} simple
\item \strong{Requirements:}
\itemize{
\item Reaction times should be passed in seconds
}
}
\itemize{
\item For version 'simple': response variable should be integer-coded (1 = correct, 2:K = errors)
\item For version 'custom': response variable should contain character labels matching formula parameter names
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{correct}: meanlog for correct accumulator
\item \code{error}: meanlog for error accumulators
\item \code{ndt}: non-decision time
\item \code{s}: sdlog (shared across accumulators)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{s} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item correct = identity; error = identity; ndt = log; s = log
}
\item \strong{Default priors:}
\itemize{
\item \code{correct}:
\itemize{
\item \code{main}: normal(-1, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{error}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-2, 0.3)
\item \code{effects}: normal(0, 0.3)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0, 0.3)
\item \code{effects}: normal(0, 0.2)
}
}
}
}
\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# simple version with 2 alternatives
dat <- rlnr(n = 500, m = c(-1, 0), s = c(1, 1), ndt = 0.2)
model <- lnr(rt = "rt", response = "response", n_alternatives = 2)
formula <- bmf(correct ~ 1, error ~ 1, ndt ~ 1)
fit <- bmm(formula, dat, model, cores = 4, backend = "cmdstanr")

# custom version with named categories
model2 <- lnr(rt = "rt", response = "resp", version = "custom",
              num_alternatives = c(target = 1, similar = 3, other = 5))
formula2 <- bmf(target ~ 1, similar ~ 1, other ~ 1, ndt ~ 1)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dlnr]{dlnr()}} and \code{\link[=rlnr]{rlnr()}} for the density and random generation
functions.
}
\keyword{bmmodel}
