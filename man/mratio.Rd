% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{mratio}
\alias{mratio}
\title{Metacognitive efficiency (M-ratio) from a fitted meta-d' SDT model}
\usage{
mratio(fit, conditions = NULL, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}} from an SDT model.}

\item{conditions}{Optional data frame of predictor values for which to
compute the ROC curve. Column names must match predictor variables used in
the formula. If \code{NULL} (default), unique predictor combinations are derived
from the data.}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles for the
credible interval (default \code{c(0.025, 0.975)}).}

\item{...}{Additional arguments passed to \code{\link[brms:posterior_linpred.brmsfit]{brms::posterior_linpred()}}.}
}
\value{
A data frame of class \code{"bmm_sdt_mratio"} summarising the posterior:
columns \code{parameter} (\code{"mratio"} or \code{"metad"}), \code{mean}, \code{median}, \code{lower},
\code{upper} (the credible-interval bounds at \code{probs}), and any condition
columns. The full per-draw posteriors are kept in the \code{draws} attribute
(long format: \code{parameter}, \code{value}, \code{.draw}, plus condition columns) for
downstream computation such as plotting or condition contrasts.
}
\description{
Extracts the posterior of the M-ratio (\eqn{\mathrm{meta\text{-}d'}/d'}) and
of meta-d' from a \code{\link[=sdt_rating]{sdt_rating()}} model fit with \code{version = "metad"}. That
model estimates the log M-ratio (\code{logmratio}) directly, so the M-ratio is
\code{exp(logmratio)} and meta-d' is \code{exp(logmratio) * dprime}. The M-ratio is the
standard measure of metacognitive efficiency (Maniscalco & Lau, 2014;
Fleming, 2017): 1 is ideal metacognition, below 1 is inefficiency, and above
1 is hyper-efficiency.
}
\references{
Maniscalco, B., & Lau, H. (2014). Signal detection theory analysis of type 1
and type 2 data: meta-d', response-specific meta-d', and the unequal
variance SDT model. In S. M. Fleming & C. D. Frith (Eds.), \emph{The
cognitive neuroscience of metacognition} (pp. 25--66). Springer.

Fleming, S. M. (2017). HMeta-d: hierarchical Bayesian estimation of
metacognitive efficiency from confidence ratings. \emph{Neuroscience of
Consciousness}, \emph{2017}(1), nix007. \doi{10.1093/nc/nix007}
}
\seealso{
\code{\link[=sdt_rating]{sdt_rating()}}, \code{\link[=latent_sdt]{latent_sdt()}}
}
