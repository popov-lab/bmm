% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_observables.R
\name{pp_observables}
\alias{pp_observables}
\alias{pp_simulate}
\title{Declare the observables of a bmm model for posterior predictive checks}
\usage{
pp_observables(model)

pp_simulate(model, prep)
}
\arguments{
\item{model}{A \code{bmmodel} object.}

\item{prep}{A \code{brmsprep} object from \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}}.}
}
\description{
\code{pp_observables()} returns the model's observable declaration used by
\code{\link[=pp_check.bmmfit]{pp_check.bmmfit()}} when \code{resp_var} is specified, or \code{NULL} for models
that delegate fully to \code{\link[brms:pp_check]{brms::pp_check()}}. \code{pp_simulate()} draws all
observables jointly from the posterior predictive distribution.
}
\details{
A \code{pp_observables()} method returns \code{list(observed, checks)}:
\itemize{
\item \code{observed}: named character vector mapping observable names to slots of
the brms standata (\code{"Y"}, \code{"vreal1"}, \code{"vint1"}, \code{"trials"}, \code{"dec"}).
The observable mapped to \code{"Y"} is the default check.
\item \code{checks}: named list of entries built by the internal \code{.pp_observable()}
constructor. Each \code{compute} closure receives a named list keyed by
\code{names(observed)} and must be elementwise, so the identical closure
produces \code{y} from length-N vectors and \code{yrep} from ndraws x N matrices.
}

A \code{pp_simulate()} method returns a named list of ndraws x nobs matrices
drawn jointly, typically through the internal \code{.pp_simulate_joint()} helper
around the model's \verb{r*()} function. Simulating observables independently
would break their joint distribution (e.g. rt and response under the DDM).
Names not in \code{observed} are ignored; declared observables that are not
simulated (design quantities such as trial counts) are filled in from the
data.

Register exactly one method per model at the most general class level
where the declaration is identical across versions.
}
\keyword{developer}
\keyword{internal}
