% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_mafc_dist}
\alias{sdt_mafc_dist}
\alias{dsdt_mafc}
\alias{rsdt_mafc}
\title{Distribution functions for m-AFC SDT}
\usage{
dsdt_mafc(n_correct, n_trials, dprime, m = NULL, log = FALSE)

rsdt_mafc(n_per_cell, n_subjects, dprime, m = NULL)
}
\arguments{
\item{n_correct}{Integer vector. Number of correct responses.}

\item{n_trials}{Integer vector. Total number of trials.}

\item{dprime}{Numeric. Sensitivity parameter.}

\item{m}{Integer. Number of alternatives. Must be >= 2. For 2-AFC, uses
the closed-form solution \code{Phi(dprime / sqrt(2))}. For m >= 3, uses
Gauss-Hermite quadrature.}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per subject.}

\item{n_subjects}{Integer. Number of subjects.}
}
\value{
\code{dsdt_mafc} returns the (log-)density (binomial probability).
\code{rsdt_mafc} returns a data frame with columns \code{id}, \code{n_trials}, and
\code{n_correct}.
}
\description{
Density and random generation for m-alternative forced choice
signal detection theory (DeCarlo, 2012). Models accuracy in tasks where
one of m alternatives contains the signal. Only the \code{dprime} parameter
is estimated (no criterion).
}
\examples{
# 4-AFC density
dsdt_mafc(n_correct = 80, n_trials = 100, dprime = 1.5, m = 4)
# Generate 4-AFC data
dat <- rsdt_mafc(n_per_cell = 200, n_subjects = 20, dprime = 1.5, m = 4)
head(dat)
}
\references{
DeCarlo, L. T. (2012). On a signal detection approach to m-alternative
forced choice with bias, with maximum likelihood and Bayesian approaches
to estimation. \emph{Journal of Mathematical and Statistical Psychology},
\emph{11}(1), 257--282.
}
\keyword{distribution}
