% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check.R
\name{data_check_findings}
\alias{data_check_findings}
\title{Generic S3 method for model-specific pre-fit data findings}
\usage{
data_check_findings(model, data, formula)
}
\arguments{
\item{model}{A \code{bmmodel} object}

\item{data}{The user supplied data.frame, before any transformations by
\code{\link[=check_data]{check_data()}}}

\item{formula}{The user supplied \code{bmmformula}}
}
\value{
A list of findings (possibly empty), each built with
\code{\link[=data_check_finding]{data_check_finding()}}
}
\description{
Called by \code{\link[=bmm_data_check]{bmm_data_check()}} to collect model-specific
diagnostics of common data coding mistakes. Like \code{\link[=check_data]{check_data()}}, methods
dispatch over the model class chain from general to specific, and every
method should concatenate its own findings with \code{NextMethod()}. Each
finding is a list with elements \code{severity} ("warning" or "note") and
\code{message}. Unlike \code{\link[=check_data]{check_data()}}, these diagnostics never throw - they
only describe likely problems, so a report can always be produced.

Defining a method is entirely optional: the default method returns an
empty list, and \code{\link[=bmm_data_check]{bmm_data_check()}} produces its full generic report for
models without any method. Add one only when a model has a common data
mistake that the hard checks in \code{\link[=check_data]{check_data()}} deliberately tolerate - a
mistake \code{\link[=check_data]{check_data()}} already warns about needs no method, because
\code{\link[=bmm_data_check]{bmm_data_check()}} captures that warning and reports it under
"Hard checks".
}
\examples{
# a method for a model whose check_data() tolerates unnormalized weights
data_check_findings.my_model <- function(model, data, formula) {
  weights <- data[[model$other_vars$weights]]
  findings <- list()
  if (!isTRUE(all.equal(sum(weights), 1))) {
    findings <- list(data_check_finding(
      "warning",
      glue::glue("The weights sum to {sum(weights)} rather than 1.")
    ))
  }
  c(findings, NextMethod())
}
}
\seealso{
\code{\link[=data_check_finding]{data_check_finding()}}, \code{\link[=bmm_data_check]{bmm_data_check()}}
}
\keyword{developer}
\keyword{internal}
