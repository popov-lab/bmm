% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_ranking_dist}
\alias{sdt_ranking_dist}
\alias{dsdt_ranking}
\alias{rsdt_ranking}
\title{Distribution functions for Ranking SDT}
\usage{
dsdt_ranking(
  counts,
  m,
  d,
  sdratio = 1,
  dist = c("gumbel_min", "normal"),
  log = FALSE
)

rsdt_ranking(n, n_trials, m, d, sdratio = 1, dist = c("gumbel_min", "normal"))
}
\arguments{
\item{counts}{Integer matrix with one row per observation and one rank-count
column per rank position (1 = most likely target), or a vector for a
single observation. Columns beyond a row's set size \code{m} must be 0.}

\item{m}{Integer vector. Number of ranked items per observation. Must be
at least 2 and no larger than the number of count columns.}

\item{d}{Numeric vector. Sensitivity: the distance between the target and
lure distributions. For \code{dist = "normal"} this is the balanced index
\eqn{d_a} that \code{\link[=sdt_yn]{sdt_yn()}} reports (in root-mean-square SD units); for
\code{dist = "gumbel_min"} the two distributions share a scale and \code{d} is the
\eqn{g'} of Meyer-Grant et al. (2025).}

\item{sdratio}{Numeric vector. Ratio of signal to noise standard deviations
(default 1, i.e., equal variance). Must be positive. Only used when
\code{dist = "normal"}.}

\item{dist}{Character. The distribution assumed for the latent evidence:
"gumbel_min" (default), the smallest extreme value distribution with
cumulative distribution function \eqn{1 - \exp(-\exp(x))}, evaluated in
closed form; or "normal", Gaussian UV-SDT by numerical integration.}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n}{Integer. Number of observations to generate. \code{n_trials}, \code{m},
\code{d}, and \code{sdratio} are recycled to this length.}

\item{n_trials}{Integer vector. Number of ranking trials per observation.}
}
\value{
\code{dsdt_ranking} returns the (log-)density (multinomial probability).
\code{rsdt_ranking} returns an integer matrix with one row per observation and
one rank-count column per rank position (\code{rank1} ... \verb{rank max(m)}); rows
with a smaller set size have structural zeros in the surplus columns,
matching the wide format \code{\link[=sdt_ranking]{sdt_ranking()}} expects.
}
\description{
Density and random generation for ranking signal detection
theory (Meyer-Grant et al., 2025). Models rank ordering of m items by
perceived strength. Only \code{d} is estimated (no criterion or stimulus
column). Supports Gumbel-min (closed form) and Gaussian UV-SDT
(numerical integration).
}
\examples{
# Gumbel-min ranking density
dsdt_ranking(counts = c(40, 30, 20, 10), m = 4, d = 1.0)
# Generate ranking data (m=4, Gumbel-min) for 10 subjects
dat <- data.frame(id = 1:10, set_size = 4L)
dat <- cbind(dat, rsdt_ranking(10, 100, m = 4, d = 1.0))
head(dat)
}
\references{
Meyer-Grant, C. G., Kellen, D., Harding, S. M., & Singmann, H. (2025).
\emph{Extreme-value signal detection theory for recognition memory: The
parametric road not taken}. PsyArXiv preprint.
\doi{10.31234/osf.io/qhrfj}
}
\keyword{distribution}
