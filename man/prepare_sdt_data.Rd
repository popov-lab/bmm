% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-data.R
\name{prepare_sdt_data}
\alias{prepare_sdt_data}
\title{Prepare Trial-Level Data for SDT Models}
\usage{
prepare_sdt_data(
  data,
  stimulus,
  response = NULL,
  confidence = NULL,
  accuracy = NULL,
  id_cols = NULL,
  outcome = c("rating", "binary"),
  n_ratings = NULL,
  response_levels = c(0, 1)
)
}
\arguments{
\item{data}{A data frame in trial-level format.}

\item{stimulus}{Column name coding the stimulus type. Must be coded as
0 (noise/new) and 1 (signal/old).}

\item{response}{Optional column name. For binary outcomes, this should code
the observed old/new response. For rating outcomes with no \code{confidence}
column, it should contain the combined rating category (1 to \code{n_ratings}).}

\item{confidence}{Optional column name with confidence ratings. When
supplied, combined SDT rating categories are created via
\code{\link[=combine_sdt_response]{combine_sdt_response()}}.}

\item{accuracy}{Optional column name with correctness coded as 0/1. This can
be used instead of \code{response}; the observed response side is then derived
from \code{stimulus} and \code{accuracy}.}

\item{id_cols}{Optional character vector of grouping columns (for example
subject or condition identifiers). The output is aggregated over
\code{id_cols} and \code{stimulus}.}

\item{outcome}{Character. Either \code{"rating"} (default) or \code{"binary"}.}

\item{n_ratings}{Optional integer. For rating outcomes, the total number of
response categories. If omitted, it is inferred from the data.}

\item{response_levels}{Optional length-2 vector defining the coding of the
observed response variable when \code{response} is binary but not coded as 0/1.}
}
\value{
A data frame in the count format required by the corresponding SDT
model family.
}
\description{
Aggregates trial-level SDT data into the wide count formats
expected by \code{\link[=sdt_binary]{sdt_binary()}} and the rating-family SDT models.
}
\keyword{transform}
