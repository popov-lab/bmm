% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-parameters.R
\name{native_parameters}
\alias{native_parameters}
\title{Posterior draws of model parameters on the native scale}
\usage{
native_parameters(
  x,
  newdata = NULL,
  pars = NULL,
  re_formula = NULL,
  scale = c("native", "sampling"),
  ndraws = NULL,
  draw_ids = NULL,
  summary = FALSE,
  prob = 0.95,
  robust = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}}.}

\item{newdata}{Data to evaluate the parameters on. If \code{NULL} (the default),
the unique observed combinations of the parameters' predictors are used,
taken as rows of the model's own data. Columns you do not supply are filled
from the first row of the model data.}

\item{pars}{Character vector of parameters to return. If \code{NULL} (the default),
all parameters of the model are returned, including those fixed to a
constant. This filters the output only; parameters transformed jointly are
always computed together.}

\item{re_formula}{Which group-level effects to include, as in
\code{\link[brms:posterior_linpred]{brms::posterior_linpred()}}. See Details.}

\item{scale}{Either \code{"native"} (the default) to apply the model's inverse link
functions, or \code{"sampling"} to return the untransformed linear predictor.}

\item{ndraws}{Number of posterior draws to use. If \code{NULL} (the default), all
draws are used. The same draws are used for every parameter.}

\item{draw_ids}{Indices of the draws to use. Overrides \code{ndraws} and makes the
result reproducible.}

\item{summary}{Logical. If \code{FALSE} (the default), return draws. If \code{TRUE},
return posterior summaries computed \emph{after} the transformation.}

\item{prob}{Probability mass of the credible interval when \code{summary = TRUE}.}

\item{robust}{Logical. If \code{TRUE}, \code{summary = TRUE} reports the median and
median absolute deviation instead of the mean and standard deviation.}

\item{...}{Further arguments passed to \code{\link[brms:posterior_linpred]{brms::posterior_linpred()}}, such as
\code{allow_new_levels} and \code{sample_new_levels}.}
}
\value{
If \code{summary = FALSE}, a \code{data.frame} with one row per draw, grid cell
and parameter, with columns \code{.chain}, \code{.iteration}, \code{.draw}, the grid
variables, \code{parameter} and \code{value}. The draw indices refer to the draws of
\code{x}, so they can be joined with the output of \code{posterior::as_draws_df(x)}.
If \code{summary = TRUE}, a \code{data.frame} with one row per grid cell and
parameter, with columns \code{Estimate}, \code{Est.Error} and the interval bounds.
}
\description{
\code{bmm} samples all parameters on their link scale (\code{log} for precision and
boundary parameters, \code{logit} or \code{softmax} for mixture weights, \code{tan_half} for
circular locations). \code{native_parameters()} returns posterior draws of those
parameters on the scale they are interpreted and reported on, evaluated over a
grid of predictor values.

Because draws are returned rather than summaries, any contrast is ordinary
arithmetic on the draws, with the baseline made explicit by the grid.
}
\section{Transform first, then summarise}{
Quantiles are preserved by monotone inverse links but means are not, so the
median and the credible interval of a native-scale parameter are exact while
its mean is not the inverse link of the linear predictor's mean. This function
transforms the draws and only then summarises, which is why \code{summary = TRUE}
is not the same as transforming the output of \code{\link[=summary.bmmfit]{summary.bmmfit()}}.
}

\section{Group-level effects}{
\code{re_formula = NULL} (the default) returns subject-specific parameters and adds
the grouping variables to the grid. \code{re_formula = NA} sets all group-level
effects to zero, which under a non-identity link gives the \emph{median} subject,
not the population mean. The population mean requires marginalising over the
distribution of group-level effects, which is done by predicting for a new
level:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{native_parameters(fit, newdata = transform(nd, id = "new"),
                  allow_new_levels = TRUE, sample_new_levels = "gaussian")
}\if{html}{\out{</div>}}

These three quantities differ substantially at between-subject standard
deviations typical for working memory data.
}

\section{What is returned}{
Parameters fixed to a constant are returned at that constant, transformed to
the native scale. Because \code{bmm} fixes parameters on the \emph{link} scale, the
native value can differ from the value shown by \code{\link[=parameters]{parameters()}}: the \code{ddm}
relative starting point \code{zr} is fixed at \code{0} under a \code{logit} link and is
therefore reported as \code{0.5}, and the \code{ezdm} and \code{cswald} diffusion constant \code{s}
is fixed at \code{0} under a \code{log} link and is reported as \code{1}.

Trial-level derived quantities, such as the response probabilities of the
mixture models or the category probabilities of \code{m3}, are not returned; use
\code{\link[brms:posterior_epred]{brms::posterior_epred()}} for those.

The default grid contains the observed combinations of the predictors, not
their full crossing, so cells that were never presented do not appear. A
continuous predictor therefore produces one grid cell per observed value;
supply \code{newdata} for such models.
}

\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
fit <- bmm(
  bmf(c ~ 0 + set_size, kappa ~ 1),
  data = oberauer_lin_2017,
  model = sdm(resp_error = "dev_rad")
)

# draws of c and kappa for every set size, on the native scale
np <- native_parameters(fit, re_formula = NA)
head(np)

# a contrast is plain arithmetic on the draws
c_draws <- subset(np, parameter == "c")
quantile(
  c_draws$value[c_draws$set_size == 1] - c_draws$value[c_draws$set_size == 4],
  probs = c(0.025, 0.5, 0.975)
)

# posterior summaries of the transformed draws
native_parameters(fit, re_formula = NA, summary = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=parameters]{parameters()}}, \code{\link[=native_transform]{native_transform()}}, \code{\link[=conditional_effects.bmmfit]{conditional_effects.bmmfit()}}
}
\keyword{extract_info}
