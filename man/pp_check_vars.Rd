% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_observables.R
\name{pp_check_vars}
\alias{pp_check_vars}
\title{List the posterior predictive checks available for a model fit}
\usage{
pp_check_vars(fit)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}}.}
}
\value{
A \code{data.frame} with one row per available check (columns \code{resp_var},
\code{label}, \code{default_type}, and \code{default}, flagging the observable that
\code{pp_check()} plots when \code{resp_var} is not specified), or \code{NULL} invisibly
for models without multi-observable support.
}
\description{
Lists the values \code{resp_var} accepts in \code{\link[=pp_check.bmmfit]{pp_check.bmmfit()}} for a fitted
model whose likelihood involves several observables.
}
\examples{
\dontrun{
fit <- bmm(bmmformula(drift ~ condition), data, ddm(rt = "rt", response = "response"))
pp_check_vars(fit)
pp_check(fit, resp_var = "response")
}
}
\seealso{
\code{\link[=pp_check.bmmfit]{pp_check.bmmfit()}}
}
\keyword{extract_info}
