% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_binary_dist}
\alias{sdt_binary_dist}
\alias{dsdt_binary}
\alias{rsdt_binary}
\title{Distribution functions for Binary SDT}
\usage{
dsdt_binary(
  n_old,
  n_trials,
  stimulus,
  dprime,
  criterion,
  sdratio = 1,
  dist = "normal",
  log = FALSE
)

rsdt_binary(
  n_per_cell,
  n_subjects,
  dprime,
  criterion,
  sdratio = 1,
  dist = "normal"
)
}
\arguments{
\item{n_old}{Integer vector. Number of "old"/"signal" responses.}

\item{n_trials}{Integer vector. Total number of trials per cell.}

\item{stimulus}{Integer vector (0/1). Stimulus type: 0 = noise, 1 = signal.}

\item{dprime}{Numeric. Sensitivity parameter.}

\item{criterion}{Numeric. Response bias (decision boundary location).}

\item{sdratio}{Numeric. Ratio of signal to noise standard deviations
(default 1, i.e., equal variance).}

\item{dist}{Character. Noise distribution: "normal" (default), "logistic",
"gumbel_min", or "gumbel_max".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per stimulus type per subject.}

\item{n_subjects}{Integer. Number of subjects.}
}
\value{
\code{dsdt_binary} returns the (log-)density (binomial probability).
\code{rsdt_binary} returns a data frame with columns \code{id}, \code{stimulus}, \code{n_trials},
and \code{n_old}.
}
\description{
Density and random generation for the binary signal detection
theory model, where the response is the number of "old"/"signal" responses
out of a fixed number of trials (a binomial likelihood).
}
\examples{
# Density of binary SDT data
dsdt_binary(n_old = 80, n_trials = 100, stimulus = 1,
            dprime = 1.5, criterion = 0.2)

# Vectorized over observations
dsdt_binary(n_old = c(30, 80), n_trials = c(100, 100),
            stimulus = c(0, 1), dprime = 1.5, criterion = 0.2,
            log = TRUE)
# Generate binary SDT data
dat <- rsdt_binary(n_per_cell = 100, n_subjects = 20,
                   dprime = 1.5, criterion = 0.2)
head(dat)
}
\references{
Green, D. M., & Swets, J. A. (1966). \emph{Signal detection theory and
psychophysics}. Wiley.
}
\keyword{distribution}
