% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{roc_observed}
\alias{roc_observed}
\title{Observed ROC points from a fitted rating SDT model}
\usage{
roc_observed(fit, conditions = NULL)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}} from a rating SDT model.}

\item{conditions}{Optional character vector of column names from the
original data to condition on (e.g., \code{"dataset"}).  If \code{NULL} (default),
all observations are pooled into a single ROC.}
}
\value{
A data frame of class \code{"bmm_sdt_roc_observed"} with columns \code{FA},
\code{Hit}, and any condition columns.  Rows include the K-1 interior
threshold points plus the (0, 0) and (1, 1) endpoints.
}
\description{
Computes empirical (observed) ROC points from the response count data used
to fit a rating SDT model.  Counts are pooled across all observations
within each stimulus type (and optional condition grouping) to produce
cumulative hit and false-alarm rates at each confidence threshold.
}
\seealso{
\code{\link[=roc_sdt]{roc_sdt()}}, \code{\link[=plot.bmm_sdt_roc]{plot.bmm_sdt_roc()}}
}
