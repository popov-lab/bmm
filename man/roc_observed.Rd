% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{roc_observed}
\alias{roc_observed}
\title{Observed ROC points from a fitted SDT model}
\usage{
roc_observed(fit, conditions = NULL)
}
\arguments{
\item{fit}{A \code{bmmfit} object from a rating or binary SDT model.}

\item{conditions}{Optional character vector of column names to condition on.
For rating models, \code{NULL} pools all observations into one ROC. For binary
models, \code{NULL} auto-detects the criterion-varying predictor(s).}
}
\value{
A data frame of class \code{"bmm_sdt_roc_observed"} with columns \code{FA},
\code{Hit}, and any condition columns. Rating models additionally include the
(0,0) and (1,1) endpoints.
}
\description{
Computes empirical (observed) ROC points from the response-count data used to
fit a \code{\link[=sdt_rating]{sdt_rating()}} or \code{\link[=sdt_binary]{sdt_binary()}} model. Rating models pool counts
across observations within each stimulus type (and optional condition) to
produce cumulative hit/false-alarm rates at each confidence threshold; binary
models produce one operating point per criterion level (the empirical
counterpart of the model-implied points from \code{\link[=roc_sdt]{roc_sdt()}}).
}
\seealso{
\code{\link[=roc_sdt]{roc_sdt()}}, \code{\link[=plot.bmm_sdt_roc]{plot.bmm_sdt_roc()}}
}
