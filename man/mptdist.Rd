% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{mptdist}
\alias{mptdist}
\alias{dmpt}
\alias{rmpt}
\title{Distribution functions for Multinomial Processing Tree (MPT) models}
\usage{
dmpt(x, pars, mpt_model, tree = NULL, covariates = NULL, log = TRUE, ...)

rmpt(
  n,
  size,
  pars,
  mpt_model,
  tree = NULL,
  covariates = NULL,
  unpack = FALSE,
  ...
)
}
\arguments{
\item{x}{Integer vector of length \code{K}, where \code{K} is the number of response
categories, giving the number of observed responses per category. The
order of the categories follows the branch names of the trees.}

\item{pars}{A named vector or list with the values of all latent parameters
appearing in the branch expressions of the selected tree, on the
probability scale. Parameters of a simplex group must sum to 1.}

\item{mpt_model}{A \code{bmmodel} object created with \code{\link[=mpt]{mpt()}} specifying the
model that densities or random samples should be generated for.}

\item{tree}{Character. For models with multiple trees, the name of the tree
to compute probabilities for. Can be omitted for single-tree models.}

\item{covariates}{A named vector or list with the values of all covariates
declared in the model that appear in the branch expressions of the
selected tree.}

\item{log}{Logical; if \code{TRUE} (default), densities are returned on the log
scale.}

\item{...}{can be used to pass additional values used in the branch
expressions, as an alternative to the \code{covariates} argument.}

\item{n}{Integer. Number of observations to generate data for.}

\item{size}{The total number of observations across all response categories.}

\item{unpack}{Logical; if \code{TRUE} and \code{n = 1}, returns a named vector instead
of a matrix. This allows automatic unpacking of response categories into
separate columns when used with \code{dplyr::reframe()}. Default is \code{FALSE}.}
}
\value{
\code{dmpt} gives the multinomial density of a vector of response counts,
and \code{rmpt} generates random response counts for the response categories.
}
\description{
Density and random generation functions for multinomial
processing tree models specified with \code{\link[=mpt]{mpt()}}. Please note that these
functions are currently not vectorized.
}
\note{
Unlike the densities of the circular models in this package (\code{dsdm()},
\code{dmixture2p()}, \code{dmixture3p()}, \code{dimm()}) and unlike \code{\link[stats:dmultinom]{stats::dmultinom()}},
\code{dmpt()} returns the log density by default, matching \code{\link[=dm3]{dm3()}}.
}
\examples{
tree_old <- mpt_tree("old", list(
  old = "D + (1 - D) * g",
  new = "(1 - D) * (1 - g)"
))
tree_new <- mpt_tree("new", list(
  old = "(1 - D) * g",
  new = "D + (1 - D) * (1 - g)"
))
model <- mpt(list(tree_old, tree_new), tree_id = "item_type")

dmpt(
  x = c(35, 15), pars = c(D = 0.7, g = 0.5),
  mpt_model = model, tree = "old"
)
rmpt(
  n = 10, size = 50, pars = c(D = 0.7, g = 0.5),
  mpt_model = model, tree = "new"
)
}
\references{
Batchelder, W. H., & Riefer, D. M. (1999). Theoretical and
empirical review of multinomial process tree modeling. Psychonomic
Bulletin & Review, 6(1), 57-86. https://doi.org/10.3758/BF03210812
}
\keyword{distribution}
