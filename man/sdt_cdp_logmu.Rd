% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_cdp.R
\name{sdt_cdp_logmu}
\alias{sdt_cdp_logmu}
\title{CDP category log-probability (multinomial logit)}
\usage{
sdt_cdp_logmu(
  cat,
  n_new,
  n_old,
  thresh,
  has_guess,
  dfam,
  drec,
  criterion,
  spacing,
  rcrit,
  sigmar,
  rho,
  kcrit,
  stimulus,
  ...
)
}
\arguments{
\item{cat}{Integer response-category index (canonical order: new, then
guess/know/remember blocks).}

\item{n_new, n_old}{Integer numbers of "new" and "old" confidence levels.}

\item{thresh}{Integer threshold-parameterization id (1 parsimonious,
2 equidistant, 3 log_distance).}

\item{has_guess}{Integer flag (1 if the Know/Guess split is active).}

\item{dfam, drec, criterion, spacing, rcrit, sigmar, rho, kcrit}{Model
parameters (draws-by-observation matrices supplied by brms). \code{kcrit} is
ignored when \code{has_guess} is 0. \code{spacing} is the fixed literal 0 for
log_distance, which uses the per-distance deltas in \code{...} instead.}

\item{stimulus}{Stimulus covariate (0 = new/lure, 1 = old/target).}

\item{...}{For \code{log_distance}, the per-distance threshold parameters
(\code{delta1}, ...) supplied by brms in contiguous order; otherwise unused.}
}
\value{
\code{log(p_cat)}, matching the shape of \code{dfam}.
}
\description{
R companion to the Stan \code{sdt_cdp_logmu} function. It returns
\code{log(p_cat)} for response category \code{cat} (unnormalized, exactly like the
Stan function -- the category probabilities sum to 1 analytically and
\code{softmax} absorbs the shared constant), which the \code{\link[=sdt_cdp]{sdt_cdp()}} multinomial
formula uses as the category logit. \code{brms} evaluates the non-linear
formula in R for \code{posterior_predict()} and \code{posterior_epred()}, so this
function must be on the search path; it is exported for that reason and is
not called directly. Vectorized over the draws-by-observations matrices
brms supplies.
}
\keyword{internal}
