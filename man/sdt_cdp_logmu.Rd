% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_cdp.R
\name{sdt_cdp_logmu}
\alias{sdt_cdp_logmu}
\title{CDP category log-probability (multinomial logit)}
\usage{
sdt_cdp_logmu(
  cat,
  n_new,
  n_old,
  thresh,
  has_guess,
  dprimef,
  dprimer,
  criterion,
  spacing,
  rcrit,
  sigmar,
  rho,
  kcrit,
  stimulus,
  ...
)
}
\arguments{
\item{cat}{Integer response-category index (canonical order: new, then
guess/know/remember blocks).}

\item{n_new, n_old}{Integer numbers of "new" and "old" confidence levels.}

\item{thresh}{Integer threshold-parameterization id (1 parsimonious,
2 equidistant).}

\item{has_guess}{Integer flag (1 if the Know/Guess split is active).}

\item{dprimef, dprimer, criterion, spacing, rcrit, sigmar, rho, kcrit}{Model
parameters (draws-by-observation matrices supplied by brms). \code{kcrit} is
ignored when \code{has_guess} is 0.}

\item{stimulus}{Stimulus covariate (0 = new/lure, 1 = old/target).}

\item{...}{Unused; absorbs any extra arguments.}
}
\value{
\code{log(p_cat)}, matching the shape of \code{dprimef}.
}
\description{
R companion to the Stan \code{sdt_cdp_logmu} function. It returns
\code{log(p_cat)} for response category \code{cat}, which the \code{\link[=sdt_cdp]{sdt_cdp()}} multinomial
formula uses as the category logit (so \code{softmax} recovers the CDP category
probabilities). \code{brms} evaluates the non-linear formula in R for
\code{posterior_predict()} and \code{posterior_epred()}, so this function must be on
the search path; it is exported for that reason and is not called directly.
}
\keyword{internal}
