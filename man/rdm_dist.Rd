% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{rdm_dist}
\alias{rdm_dist}
\alias{drdm}
\alias{rrdm}
\alias{prdm}
\alias{qrdm}
\title{Distribution functions for the Racing Diffusion Model (RDM)}
\usage{
drdm(rt, response, drift, bound, ndt, s = 1, A = 0, log = FALSE)

rrdm(n, drift, bound, ndt, s = 1, A = 0)

prdm(q, drift, bound, ndt, s = 1, A = 0, lower.tail = TRUE, log.p = FALSE)

qrdm(p, drift, bound, ndt, s = 1, A = 0, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{rt}{Numeric vector of response times (in seconds).}

\item{response}{Integer vector indicating which accumulator won (1-indexed).}

\item{drift}{Numeric vector of drift rates for each accumulator (all > 0).}

\item{bound}{Boundary parameter (> 0). When \code{A = 0}, this is the threshold.
When \code{A > 0}, the effective threshold is \code{A + bound}.}

\item{ndt}{Non-decision time in seconds (>= 0).}

\item{s}{Diffusion constant (> 0), default = 1.}

\item{A}{Maximum starting point for uniform variability on \verb{[0, A]} (>= 0).
Default = 0 (no starting point variability).}

\item{log}{Logical; if \code{TRUE}, values are returned on the log scale.}

\item{n}{Number of observations to generate.}

\item{q}{Numeric vector of quantiles (response times in seconds).}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are P(X <= x).}

\item{log.p}{Logical; if \code{TRUE}, probabilities are given as log(p).}

\item{p}{Numeric vector of probabilities.}
}
\value{
\itemize{
\item \code{drdm()} returns a numeric vector of (log-)densities.
\item \code{rrdm()} returns a data.frame with columns \code{rt} and \code{response}.
\item \code{prdm()} returns a numeric vector of (log-)probabilities.
\item \code{qrdm()} returns a numeric vector of quantiles (response times).
}
}
\description{
Density, random generation, CDF, and quantile functions for the
Racing Diffusion Model (Tillman, Van Zandt, & Logan, 2020). The RDM is a
multi-accumulator race model where each accumulator follows a Wald (inverse
Gaussian) distribution. The first accumulator to finish determines the
response and the RT.
}
\examples{
dat <- rrdm(n = 1000, drift = c(3, 1.5), bound = 1, ndt = 0.2)
head(dat)
hist(dat$rt)

# with starting point variability
dat2 <- rrdm(n = 1000, drift = c(3, 1.5), bound = 1, ndt = 0.2, A = 0.3)
}
\references{
Tillman, G., Van Zandt, T., & Logan, G. D. (2020). Sequential sampling
models without random between-trial variability: the racing diffusion
model of speeded decision making. Psychonomic Bulletin & Review, 27,
911-936.
}
\keyword{distribution}
