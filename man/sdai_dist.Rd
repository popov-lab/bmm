% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdai_dist}
\alias{sdai_dist}
\alias{dsdai}
\alias{rsdai}
\title{Distribution functions for the SDAI model}
\usage{
dsdai(
  tabs_counts,
  tpres_counts,
  dprime,
  criterion,
  margins_l = NULL,
  margins_h = NULL,
  sdratio = 1,
  dist = "gumbel_min",
  log = FALSE
)

rsdai(
  n_per_cell = 50,
  n_subjects = 1,
  dprime = 1,
  criterion = 0,
  margins_l = NULL,
  margins_h = NULL,
  sdratio = 1,
  dist = "gumbel_min",
  n_sources = 2,
  n_confidence = 4
)
}
\arguments{
\item{tabs_counts}{Integer vector. Response counts for the table
(discrimination) process (length = \code{n_confidence}).}

\item{tpres_counts}{Integer vector. Response counts for the test-present
(source confusion) process (length = \code{(n_sources - 1) * n_confidence}).}

\item{dprime}{Numeric. Source discrimination parameter.}

\item{criterion}{Numeric. Central decision threshold.}

\item{margins_l}{Numeric vector. Left (below-criterion) margin parameters
on the original scale (not log). Length = \code{n_confidence - 2}.
Use \code{NULL} for 2-confidence models (no margins).}

\item{margins_h}{Numeric vector. Right (above-criterion) margin parameters.
Same length as \code{margins_l}.}

\item{sdratio}{Numeric. SD ratio for Gaussian models (default 1, i.e.,
equal variance). Ignored for Gumbel models.}

\item{dist}{Character. Noise distribution: "gumbel_min" (default) or
"normal".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per cell.}

\item{n_subjects}{Integer. Number of subjects.}

\item{n_sources}{Integer. Number of source categories (default 2).}

\item{n_confidence}{Integer. Number of confidence levels (default 4).}
}
\value{
\code{dsdai} returns the (log-)density (product of two multinomial
likelihoods for tabs and tpres processes). \code{rsdai} returns a data frame
with columns \code{id} and one column per response category
(\verb{i\{source\}_c\{confidence\}}).
}
\description{
Density and random generation for the Source-Dependent
Asymmetric Information (SDAI) model (Meyer-Grant et al., 2025).
Supports both Gumbel-min and Gaussian noise distributions.
}
\examples{
# Gumbel SDAI density (2 sources x 4 confidence)
dsdai(tabs_counts = c(10, 20, 30, 40),
      tpres_counts = c(15, 25, 35, 25, 20, 30, 30, 20),
      dprime = 1.0, criterion = 0,
      margins_l = c(0.5, 0.3), margins_h = c(0.5, 0.3))
# Generate SDAI data
dat <- rsdai(n_per_cell = 100, n_subjects = 5, dprime = 1.0,
             criterion = 0, margins_l = c(0.5, 0.3),
             margins_h = c(0.5, 0.3))
head(dat)
}
\references{
Meyer-Grant, C. G., Kellen, D., & Klauer, K. C. (2025). A
Gumbel-distribution model of recognition memory.
}
\keyword{distribution}
