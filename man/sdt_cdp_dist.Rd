% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_cdp_dist}
\alias{sdt_cdp_dist}
\alias{dsdt_cdp}
\alias{rsdt_cdp}
\title{Distribution functions for Continuous Dual-Process SDT (CDP)}
\usage{
dsdt_cdp(
  counts,
  stimulus,
  dprimef,
  dprimer,
  thresholds,
  rcrit,
  kcrit = NULL,
  sigmar = 0,
  dist = "normal",
  n_new = NULL,
  n_old = NULL,
  log = FALSE
)

rsdt_cdp(
  n_per_cell,
  n_subjects,
  dprimef,
  dprimer,
  criterion,
  spacing,
  rcrit,
  kcrit = NULL,
  sigmar = 0,
  dist = "normal",
  n_ratings = NULL,
  n_new = NULL,
  n_old = NULL,
  threshold_type = "parsimonious"
)
}
\arguments{
\item{counts}{Integer vector of response counts. For 2-way R/K: length
n_new + 2 * n_old (new, know, remember blocks). For 3-way R/K/G: length
n_new + 3 * n_old (new, guess, know, remember blocks).}

\item{stimulus}{Integer. 0 (noise/new) or 1 (signal/old).}

\item{dprimef}{Numeric. Familiarity sensitivity (target mean on F axis).}

\item{dprimer}{Numeric. Recollection sensitivity (target mean on R axis).}

\item{thresholds}{Numeric vector of K-1 ordered confidence thresholds on the
aggregated F+R axis, where K = 2 * n_conf.}

\item{rcrit}{Numeric. Remember criterion on the recollection axis.}

\item{kcrit}{Numeric or NULL. Know criterion on the familiarity axis.
NULL or -Inf for 2-way R/K model (default). Finite value enables 3-way
R/K/G model.}

\item{sigmar}{Numeric. Log SD ratio for recollection target distribution.
0 = equal variance (default).}

\item{dist}{Character. The noise distribution: "normal" (default),
"logistic", "gumbel_min", or "gumbel_max".}

\item{n_new}{Integer. Number of "new" confidence levels.}

\item{n_old}{Integer. Number of "old" confidence levels (with R/K split).
Defaults to n_new for symmetric scales.}

\item{log}{Logical. If TRUE, returns log-density (default FALSE).}

\item{n_per_cell}{Integer. Number of trials per stimulus condition per
subject.}

\item{n_subjects}{Integer. Number of subjects.}

\item{criterion}{Numeric. Central confidence criterion for threshold
generation.}

\item{spacing}{Numeric. Threshold spacing parameter.}

\item{n_ratings}{Integer. Total number of confidence levels (must be even).}

\item{threshold_type}{Character. "parsimonious" (default) or "equidistant".}
}
\value{
\code{dsdt_cdp} returns the (log-)density (multinomial probability).
\code{rsdt_cdp} returns a data frame in wide format with columns: \code{id},
\code{stimulus}, response count columns, and \code{nTrials}.
}
\description{
Density and random generation for the continuous dual-process
signal detection theory model (Wixted & Mickes, 2010). The model assumes
two independent continuous dimensions — Familiarity (F) and Recollection
(R). Old/new confidence ratings are based on the aggregated signal F + R.
Remember/Know judgments are based on R exceeding a criterion. Optionally,
a Know/Guess split uses F exceeding a second criterion.

When only confidence rating data is available (no R/K split), the CDP model
reduces to UV-SDT on the aggregated F + R axis. Use \code{\link[=sdt_rating]{sdt_rating()}} for
that case.
}
\examples{
# CDP density (2-way R/K, 6-point scale)
dsdt_cdp(
  counts = c(50, 30, 20, 10, 15, 25, 5, 20, 75),
  stimulus = 1, dprimef = 0.8, dprimer = 1.0,
  thresholds = c(-1.5, -0.5, 0.0, 0.5, 1.5),
  rcrit = 0.5
)
# Generate CDP data (2-way R/K)
dat <- rsdt_cdp(n_per_cell = 100, n_subjects = 10,
                dprimef = 0.8, dprimer = 1.0,
                criterion = 0, spacing = 0.5,
                rcrit = 0.5, n_ratings = 6)
}
\references{
Wixted, J. T., & Mickes, L. (2010). A continuous dual-process model of
remember/know judgments. \emph{Psychological Review}, \emph{117}(4),
1025--1054. \doi{10.1037/a0020874}
}
\keyword{distribution}
