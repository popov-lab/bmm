% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lba.R
\name{lba}
\alias{lba}
\title{Linear Ballistic Accumulator}
\usage{
lba(
  rt,
  response,
  n_alternatives = NULL,
  version = c("simple", "custom"),
  distribution = c("normal", "gamma", "frechet", "lognormal"),
  num_alternatives = NULL,
  links = NULL,
  ...
)
}
\arguments{
\item{rt}{The name of the variable in the dataset containing the response
times. Response times should be coded in seconds (not milliseconds).}

\item{response}{The name of the variable in the dataset containing the
response/choice. For the \code{"simple"} version, responses should be
integer-coded: 1 = correct response, 2 through K = error responses.
Factor and character-digit responses are accepted and converted
automatically. For the \code{"custom"} version, responses should be character
or factor labels matching the accumulator names in the formula.}

\item{n_alternatives}{An integer specifying the total number of response
alternatives (K >= 2). Required for \code{version = "simple"}. Not used for
\code{version = "custom"} (inferred from the formula).}

\item{version}{A character string specifying which version of the LBA model
to use. Options are:
\itemize{
\item \code{"simple"} (default): Two drift parameters — one for the correct
accumulator (response = 1) and one for all error accumulators. The
scale parameter \code{s} is shared and fixed by default (s = 1). This
covers the common case where interest is in the speed of correct vs.
error processing.
\item \code{"custom"}: Per-category drift parameters. Response categories
are defined by the formula LHS names (e.g., \verb{cat1 ~ 1, cat2 ~ 1}).
The response column must contain character labels matching these names.
Supports per-category \code{num_alternatives}.
}}

\item{distribution}{A character string specifying the trial-to-trial drift
rate distribution. All distributions use \code{driftc}/\code{drifte} as drift
parameters (for the simple version), but their interpretation and link
function differ:
\itemize{
\item \code{"normal"} (default): Drift rates drawn from a normal distribution.
\code{driftc}/\code{drifte} = mean drift rate (identity link), \code{s} = SD (fixed=1).
\item \code{"gamma"}: Drift rates drawn from a gamma distribution.
\code{driftc}/\code{drifte} = shape parameter (log link), \code{s} = rate (fixed=1).
\item \code{"frechet"}: Drift rates drawn from a Frechet distribution.
\code{driftc}/\code{drifte} = shape parameter (log link), \code{s} = scale (fixed=1).
\item \code{"lognormal"}: Drift rates drawn from a lognormal distribution.
\code{driftc}/\code{drifte} = meanlog parameter (identity link), \code{s} = sdlog
(fixed=1).
}}

\item{num_alternatives}{For \code{version = "custom"} only. A named vector
specifying the number of racing accumulators per response category.
Can be a named integer vector for constant counts or a named character
vector of column names for trial-varying counts. If omitted, defaults to
1 accumulator per category.}

\item{links}{A named list of link functions for the model parameters.}

\item{...}{Additional arguments passed internally (for testing purposes).}
}
\value{
An object of class \code{bmmodel}
}
\description{
Linear Ballistic Accumulator
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks (multi-alternative)
\item \strong{Name:} Linear Ballistic Accumulator
\item \strong{Citation:}
\itemize{
\item Brown, S. D., & Heathcote, A. (2008). The simplest complete
model of choice response time: Linear ballistic accumulation. Cognitive
Psychology, 57(3), 153-178. https://doi.org/10.1016/j.cogpsych.2007.12.002
}
\item \strong{Version:} simple
\item \strong{Requirements:}
\itemize{
\item Reaction times should be passed in seconds
}
}
\itemize{
\item For version 'simple': response variable should be integer-coded (1 = correct, 2:K = errors)
\item For version 'custom': response variable should contain character labels matching formula parameter names
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{driftc}: mean drift rate for correct accumulator
\item \code{drifte}: mean drift rate for error accumulators
\item \code{B}: threshold gap (b = A + B)
\item \code{A}: maximum starting point (uniform on 0 to A)
\item \code{ndt}: non-decision time
\item \code{s}: drift rate SD (fixed to 1 by default)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{s} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item driftc = identity; drifte = identity; B = log; A = log; ndt = log; s = log
}
\item \strong{Default priors:}
\itemize{
\item \code{driftc}:
\itemize{
\item \code{main}: normal(3, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{drifte}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{B}:
\itemize{
\item \code{main}: normal(-0.5, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{A}:
\itemize{
\item \code{main}: normal(-1, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-2, 0.3)
\item \code{effects}: normal(0, 0.3)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0, 0.3)
\item \code{effects}: normal(0, 0.2)
}
}
}
}
\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# simple version with 2 alternatives
dat <- rlba(n = 500, drift = c(3, 1.5), B = 0.5, A = 0.5, ndt = 0.2)
model <- lba(rt = "rt", response = "response", n_alternatives = 2)
formula <- bmf(driftc ~ 1, drifte ~ 1, B ~ 1, A ~ 1, ndt ~ 1)
fit <- bmm(formula, dat, model, cores = 4, backend = "cmdstanr")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dlba]{dlba()}} and \code{\link[=rlba]{rlba()}} for the density and random generation
functions.
}
\keyword{bmmodel}
