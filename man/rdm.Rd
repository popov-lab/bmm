% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rdm.R
\name{rdm}
\alias{rdm}
\title{Racing Diffusion Model}
\usage{
rdm(
  rt,
  response,
  n_choices = NULL,
  version = c("simple", "custom"),
  accumulators = NULL,
  links = NULL,
  ...
)
}
\arguments{
\item{rt}{The name of the variable in the dataset containing the response
times. Response times should be coded in seconds (not milliseconds).}

\item{response}{The name of the variable in the dataset containing the
response/choice. For the \code{"simple"} version, responses should be
integer-coded: 1 = correct response, 2 through K = error responses.
Factor and character-digit responses are accepted and converted
automatically. For the \code{"custom"} version, responses should be character
or factor labels matching the accumulator names in the formula.}

\item{n_choices}{An integer specifying the total number of response
alternatives (K >= 2). Required for \code{version = "simple"}. Not used for
\code{version = "custom"} (inferred from the formula).}

\item{version}{A character string specifying which version of the RDM to
use. Options are:
\itemize{
\item \code{"simple"} (default): Two drift parameters — \code{driftc} for the
correct accumulator (response = 1) and \code{drifte} for all error
accumulators. The diffusion constant \code{s} is shared and fixed by
default (s = 1). The start-point range \code{sp} is fixed to zero by
default; add \code{sp ~ 1} to the formula to estimate it.
\item \code{"custom"}: Per-category drift parameters. Response categories
are defined by the formula LHS names (e.g., \verb{correct ~ 1, other ~ 1, npl ~ 1}). The response column must contain character labels matching
these names. Supports per-category \code{accumulators}.
}}

\item{accumulators}{For \code{version = "custom"} only. A named vector
specifying the number of racing accumulators per response category.
Can be a named integer vector for constant counts (e.g.,
\code{c(correct = 1, other = 3, npl = 5)}) or a named character vector
of column names for trial-varying counts (e.g.,
\code{c(correct = "n_corr", other = "n_other", npl = "n_npl")}). If omitted,
defaults to 1 accumulator per category.}

\item{links}{A named list of link functions for the model parameters.
For \code{"simple"}: parameters are \code{driftc}, \code{drifte}, \code{gap}, \code{ndt}, \code{s},
and \code{sp}. All positive-valued parameters, including \code{ndt}, use a "log"
link and only support that link.}

\item{...}{Additional arguments passed internally (for testing purposes).}
}
\value{
An object of class \code{bmmodel}
}
\description{
Racing Diffusion Model
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks (multi-alternative)
\item \strong{Name:} Racing Diffusion Model
\item \strong{Citation:}
\itemize{
\item Tillman, G., Van Zandt, T., & Logan, G. D. (2020).
Sequential sampling models without random between-trial variability:
the racing diffusion model of speeded decision making. Psychonomic
Bulletin & Review, 27, 911-936. https://doi.org/10.3758/s13423-020-01719-6
}
\item \strong{Version:} simple
\item \strong{Requirements:}
\itemize{
\item Reaction times should be passed in seconds
}
}
\itemize{
\item For version 'simple': response variable should be integer-coded (1 = correct, 2:K = errors)
\item For version 'custom': response variable should contain character labels matching formula parameter names
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{driftc}: drift rate for correct accumulator
\item \code{drifte}: drift rate for error accumulators
\item \code{gap}: threshold gap (b = gap + sp)
\item \code{ndt}: non-decision time
\item \code{s}: diffusion constant
\item \code{sp}: maximum starting point (uniform on 0 to sp)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{s} = 0
\item \code{sp} = -100
}
\item \strong{Default parameter links:}
\itemize{
\item driftc = log; drifte = log; gap = log; ndt = log; s = log; sp = log
}
\item \strong{Default priors:}
\itemize{
\item \code{driftc}:
\itemize{
\item \code{main}: normal(1, 0.5)
\item \code{effects}: normal(0, 0.3)
\item \code{sd}: exponential(2)
}
\item \code{drifte}:
\itemize{
\item \code{main}: normal(0.5, 0.5)
\item \code{effects}: normal(0, 0.3)
\item \code{sd}: exponential(2)
}
\item \code{gap}:
\itemize{
\item \code{main}: normal(0, 0.3)
\item \code{effects}: normal(0, 0.3)
\item \code{sd}: exponential(2)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-1.5, 0.5)
\item \code{effects}: normal(0, 0.3)
\item \code{sd}: exponential(4)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0, 0.3)
\item \code{effects}: normal(0, 0.2)
\item \code{sd}: exponential(4)
}
\item \code{sp}:
\itemize{
\item \code{main}: normal(-1, 0.5)
\item \code{effects}: normal(0, 0.3)
\item \code{sd}: exponential(2)
}
}
}

The fixed-parameter values above are on the log link: \code{s = 0} is a
diffusion constant of 1, and \code{sp = -100} is a starting-point range of
essentially 0, i.e. no start-point variability.

\subsection{Start-point variability (\code{sp})}{

Each accumulator starts at a point drawn uniformly from \verb{[0, sp]} and
finishes when it has travelled \code{gap + sp} minus that start; \code{sp} is the
range of the start point and \code{gap} the distance from its top to the
threshold, so the threshold is \code{b = gap + sp}. By default \code{sp} is fixed to
\code{-100} on the log link, i.e. to zero: every accumulator starts at zero and
its finishing time is a plain Wald with threshold \code{gap}. Write \code{sp ~ 1} (or
a predictor) in \code{bmf()} to estimate it, or fix it to another range with a
constant on the log link, e.g. \code{bmf(..., sp = log(0.3))}, which is honoured
as a start point uniform on \verb{[0, 0.3]} by the sampler and by \code{log_lik()},
\code{posterior_predict()}, \code{posterior_epred()} and \code{pp_check()} alike.
}

\subsection{Likelihood convention}{

\code{log_lik()}, and therefore \code{loo()} and \code{waic()}, use the likelihood of the
\emph{category} that was observed, not of one named accumulator: the winner's
density carries \code{log(n_j)} for a category raced by \code{n_j} identical
accumulators. For \code{version = "simple"} the error category holds all K - 1
error accumulators, so every error trial sits log(K - 1) above a
per-response likelihood. That term does not depend on any parameter, so
posteriors and predictions are unaffected, but \code{loo()} and \code{waic()} values
are shifted by it and are not comparable with a \code{version = "custom"} fit,
which names each accumulator, or with another package that does.
}
}
\note{
Both versions describe the same response type (a categorical winner in
a choice-RT race), so they live in one constructor rather than separate
model functions: \code{"simple"} is an accuracy-coded convenience layer (correct
vs. error) over the general per-accumulator case handled by \code{"custom"}.
}
\section{Default priors}{

\code{driftc}/\code{drifte} get \code{normal(1, 0.5)}/\code{normal(0.5, 0.5)}, and \code{gap}/\code{sp}
get \code{normal(0, 0.3)}/\code{normal(-1, 0.5)}, all on the log link.

\code{ndt} has \code{normal(-1.5, 0.5)} on the log link, the prior the \strong{ddm} model
uses, rather than an earlier \code{normal(-2, 0.3)} whose 95\% interval put
\code{ndt} below 0.244 s and gave P(ndt > 0.3 s) = 0.004, too tight for
multi-alternative choice tasks.

The group-level standard deviations get one rate per kind of parameter
rather than one per model, shared with the \strong{lnr} and \strong{lba} models: a
drift rate gets \code{exponential(2)} (median 0.35 on the log link, i.e. a
between-subject ratio around 1.4), \code{ndt} and \code{s} get \code{exponential(4)}
(median 0.17), and \code{gap}/\code{sp} get \code{exponential(2)}. These rates come from
hierarchical fits of simulated multi-subject data for the racing models
(drift/\code{ndt}/\code{s} anchored on the \strong{lnr} recovery, \code{gap}/\code{sp} reused from
the \strong{lba} model).
}

\section{Identifiability of \code{s}}{

The likelihood is invariant to scaling every drift rate, \code{gap}, \code{sp} and
\code{s} by the same positive constant, so only their ratios are identified.
\code{s} is fixed to 1 by default (\code{s = 0} on the log link) to pin that scale.
Freeing \code{s} with an intercept estimates that scale ray rather than a new
quantity, and \code{check_formula()} warns when a formula does so; write
\code{s ~ 0 + condition} instead to estimate contrasts of \code{s} without freeing
the scale.
}

\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# simple version with 2 alternatives
dat <- rrdm(n = 500, drift = c(3, 1.5), gap = 1, sp = 0, ndt = 0.2)
model <- rdm(rt = "rt", response = "response", n_choices = 2)
formula <- bmf(driftc ~ 1, drifte ~ 1, gap ~ 1, ndt ~ 1)
fit <- bmm(formula, dat, model, cores = 4, backend = "cmdstanr")

# with starting point variability
formula2 <- bmf(driftc ~ 1, drifte ~ 1, gap ~ 1, ndt ~ 1, sp ~ 1)
fit2 <- bmm(formula2, dat, model, cores = 4, backend = "cmdstanr")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=drdm]{drdm()}} and \code{\link[=rrdm]{rrdm()}} for the density and random generation
functions.
}
\keyword{bmmodel}
