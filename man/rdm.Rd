% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_rdm.R
\name{rdm}
\alias{rdm}
\title{Racing Diffusion Model}
\usage{
rdm(
  rt,
  response,
  n_alternatives = NULL,
  version = c("simple", "custom"),
  num_alternatives = NULL,
  links = NULL,
  ...
)
}
\arguments{
\item{rt}{The name of the variable in the dataset containing the response
times. Response times should be coded in seconds (not milliseconds).}

\item{response}{The name of the variable in the dataset containing the
response/choice. For the \code{"simple"} version, responses should be
integer-coded: 1 = correct response, 2 through K = error responses.
Factor and character-digit responses are accepted and converted
automatically. For the \code{"custom"} version, responses should be character
or factor labels matching the accumulator names in the formula.}

\item{n_alternatives}{An integer specifying the total number of response
alternatives (K >= 2). Required for \code{version = "simple"}. Not used for
\code{version = "custom"} (inferred from the formula).}

\item{version}{A character string specifying which version of the RDM to
use. Options are:
\itemize{
\item \code{"simple"} (default): Two drift parameters — \code{driftc} for the
correct accumulator (response = 1) and \code{drifte} for all error
accumulators. The diffusion constant \code{s} is shared and fixed by
default (s = 1). Starting point proportion \code{sp} is fixed to ~0 by
default; add \code{sp ~ 1} to the formula to estimate it.
\item \code{"custom"}: Per-category drift parameters. Response categories
are defined by the formula LHS names (e.g., \verb{correct ~ 1, other ~ 1, npl ~ 1}). The response column must contain character labels matching
these names. Supports per-category \code{num_alternatives}.
}}

\item{num_alternatives}{For \code{version = "custom"} only. A named vector
specifying the number of racing accumulators per response category.
Can be a named integer vector for constant counts (e.g.,
\code{c(correct = 1, other = 3, npl = 5)}) or a named character vector
of column names for trial-varying counts (e.g.,
\code{c(correct = "n_corr", other = "n_other", npl = "n_npl")}). If omitted,
defaults to 1 accumulator per category.}

\item{links}{A named list of link functions for the model parameters.
For \code{"simple"}: parameters are \code{driftc}, \code{drifte}, \code{bound}, \code{ndt}, \code{s},
and \code{sp}. Default links are "log" for all except \code{sp} which uses "logit".}

\item{...}{Additional arguments passed internally (for testing purposes).}
}
\value{
An object of class \code{bmmodel}
}
\description{
Racing Diffusion Model
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks (multi-alternative)
\item \strong{Name:} Racing Diffusion Model
\item \strong{Citation:}
\itemize{
\item Tillman, G., Van Zandt, T., & Logan, G. D. (2020).
Sequential sampling models without random between-trial variability:
the racing diffusion model of speeded decision making. Psychonomic
Bulletin & Review, 27, 911-936. https://doi.org/10.3758/s13423-020-01719-6
}
\item \strong{Version:} simple
\item \strong{Requirements:}
\itemize{
\item Reaction times should be passed in seconds
}
}
\itemize{
\item For version 'simple': response variable should be integer-coded (1 = correct, 2:K = errors)
\item For version 'custom': response variable should contain character labels matching formula parameter names
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{driftc}: drift rate for correct accumulator
\item \code{drifte}: drift rate for error accumulators
\item \code{bound}: decision threshold
\item \code{ndt}: non-decision time
\item \code{s}: diffusion constant
\item \code{sp}: starting point proportion (A/bound, on logit scale)
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{s} = 0
\item \code{sp} = -100
}
\item \strong{Default parameter links:}
\itemize{
\item driftc = log; drifte = log; bound = log; ndt = log; s = log; sp = logit
}
\item \strong{Default priors:}
\itemize{
\item \code{driftc}:
\itemize{
\item \code{main}: normal(1, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{drifte}:
\itemize{
\item \code{main}: normal(0.5, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{bound}:
\itemize{
\item \code{main}: normal(0, 0.5)
\item \code{effects}: normal(0, 0.3)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-2, 0.3)
\item \code{effects}: normal(0, 0.3)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0, 0.3)
\item \code{effects}: normal(0, 0.2)
}
\item \code{sp}:
\itemize{
\item \code{main}: normal(-1, 1)
\item \code{effects}: normal(0, 0.5)
}
}
}
}
\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# simple version with 2 alternatives
dat <- rrdm(n = 500, drift = c(3, 1.5), bound = 1, ndt = 0.2)
model <- rdm(rt = "rt", response = "response", n_alternatives = 2)
formula <- bmf(driftc ~ 1, drifte ~ 1, bound ~ 1, ndt ~ 1)
fit <- bmm(formula, dat, model, cores = 4, backend = "cmdstanr")

# with starting point variability
formula2 <- bmf(driftc ~ 1, drifte ~ 1, bound ~ 1, ndt ~ 1, sp ~ 1)
fit2 <- bmm(formula2, dat, model, cores = 4, backend = "cmdstanr")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=drdm]{drdm()}} and \code{\link[=rrdm]{rrdm()}} for the density and random generation
functions.
}
\keyword{bmmodel}
