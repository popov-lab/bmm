% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_metad_dist}
\alias{sdt_metad_dist}
\alias{dsdt_metad}
\alias{rsdt_metad}
\title{Distribution functions for meta-d' SDT}
\usage{
dsdt_metad(
  counts,
  stimulus,
  dprime,
  thresholds,
  metad,
  sdratio = 1,
  dist = "normal",
  log = FALSE
)

rsdt_metad(
  n_per_cell,
  n_subjects,
  dprime,
  criterion,
  metad,
  sdratio = 1,
  dist = "normal",
  n_ratings = NULL,
  spacing = NULL,
  deltas = NULL,
  threshold_type = "parsimonious"
)
}
\arguments{
\item{counts}{Integer matrix with one row per observation and one column
per rating category, ordered from "definitely noise" (1) to "definitely
signal" (K), or a vector for a single observation.}

\item{stimulus}{Integer vector (0/1). Stimulus type: 0 = noise, 1 = signal.}

\item{dprime}{Numeric vector. Sensitivity parameter(s).}

\item{thresholds}{Numeric vector of length K-1 with the ordered decision
thresholds, or an n-by-(K-1) matrix with one row per observation.}

\item{metad}{Numeric. Metacognitive sensitivity (type-2 d'). \code{metad = dprime}
corresponds to ideal metacognition (recovers rating SDT).}

\item{sdratio}{Numeric vector. Ratio of signal to noise standard deviations
(default 1, i.e., equal variance).}

\item{dist}{Character. Noise distribution: "normal" (default), "logistic",
"gumbel_min", or "gumbel_max".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}
}
\value{
\code{dsdt_metad} returns the (log-)density (multinomial probability).
\code{rsdt_metad} returns a data frame with columns \code{id}, \code{stimulus},
\code{r1}, ..., \code{rK}, and \code{nTrials}.
}
\description{
Density and random generation for the meta-d' model
(Maniscalco & Lau, 2012). Confidence thresholds are placed using the
metacognitive sensitivity \code{metad}, then rescaled so the total "old"/"new"
response rates match what type-1 \code{dprime} predicts. These are the simulation
counterparts of the \code{metad} version of \code{\link[=sdt_rating]{sdt_rating()}}.
}
\examples{
# Density for a single observation (K=4) with imperfect metacognition
dsdt_metad(counts = c(5, 15, 25, 55), stimulus = 1,
           dprime = 1.5, thresholds = c(-0.5, 0.0, 0.5), metad = 1.0)
# Generate meta-d' rating data (K=4)
dat <- rsdt_metad(n_per_cell = 100, n_subjects = 10, dprime = 1.5,
                  criterion = 0, metad = 1.0, n_ratings = 4, spacing = 0.5)
head(dat)
}
\references{
Maniscalco, B., & Lau, H. (2012). A signal detection theoretic approach for
estimating metacognitive sensitivity from confidence ratings.
\emph{Consciousness and Cognition}, \emph{21}(1), 422--430.
\doi{10.1016/j.concog.2011.09.021}
}
\keyword{distribution}
