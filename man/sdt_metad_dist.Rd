% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_metad_dist}
\alias{sdt_metad_dist}
\alias{dsdt_metad}
\alias{rsdt_metad}
\title{Distribution functions for Meta-d' SDT}
\usage{
dsdt_metad(
  counts,
  stimulus,
  dprime,
  metad,
  thresholds,
  sdratio = 1,
  dist = "normal",
  log = FALSE
)

rsdt_metad(
  n_per_cell,
  n_subjects,
  dprime,
  criterion,
  metad = NULL,
  sdratio = 1,
  dist = "normal",
  n_ratings = NULL,
  spacing = NULL,
  deltas = NULL,
  threshold_type = "parsimonious"
)
}
\arguments{
\item{counts}{Integer vector of length K. Response counts per rating
category for a single observation.}

\item{stimulus}{Integer (0 or 1). Stimulus type: 0 = noise, 1 = signal.}

\item{dprime}{Numeric. Type-1 sensitivity parameter.}

\item{metad}{Numeric. Metacognitive sensitivity. If \code{metad = dprime},
metacognition is ideal.}

\item{thresholds}{Numeric vector of length K-1. Ordered decision thresholds.}

\item{sdratio}{Numeric. Ratio of signal to noise standard deviations
(default 1).}

\item{dist}{Character. Noise distribution: "normal" (default), "logistic",
"gumbel_min", or "gumbel_max".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per stimulus type per subject.}

\item{n_subjects}{Integer. Number of subjects.}

\item{criterion}{Numeric. Central decision threshold.}

\item{n_ratings}{Integer. Number of response categories (must be >= 3).}

\item{spacing}{Numeric. Threshold spacing parameter.}

\item{deltas}{Numeric vector. Parameters for "log_distance" or "log_ratio"
threshold types.}

\item{threshold_type}{Character. Threshold parameterization: "parsimonious"
(default), "equidistant", "log_distance", or "log_ratio".}
}
\value{
\code{dsdt_metad} returns the (log-)density. \code{rsdt_metad} returns a data
frame with columns \code{id}, \code{stimulus}, \code{r1}, ..., \code{rK}, and \code{nTrials}.
}
\description{
Density and random generation for the meta-d' model
(Maniscalco & Lau, 2012). Uses metacognitive sensitivity (\code{metad}) for
confidence threshold placement, normalized so that total "old"/"new"
response rates match what type-1 d' predicts.
}
\examples{
# Meta-d' density (K=4)
dsdt_metad(counts = c(5, 15, 25, 55), stimulus = 1,
           dprime = 1.5, metad = 1.2,
           thresholds = c(-0.5, 0.0, 0.5))
# Generate meta-d' data
dat <- rsdt_metad(n_per_cell = 100, n_subjects = 10,
                  dprime = 1.5, criterion = 0, metad = 1.2,
                  n_ratings = 4, spacing = 0.5)
}
\references{
Maniscalco, B., & Lau, H. (2012). A signal detection theoretic approach
for estimating metacognitive sensitivity from confidence ratings.
\emph{Consciousness and Cognition}, \emph{21}(1), 422--430.
\doi{10.1016/j.concog.2011.09.021}
}
\keyword{distribution}
