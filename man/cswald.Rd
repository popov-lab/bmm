% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_cswald.R
\name{cswald}
\alias{cswald}
\title{Censored-Shifted Wald Model}
\usage{
cswald(rt, response, links = NULL, version = c("simple", "crisk"), ...)
}
\arguments{
\item{rt}{The name of the variable in the dataset containing the response
times. Response times should be coded in seconds (not milliseconds).}

\item{response}{The name of the variable in the dataset containing the
response/decision. Responses should be coded as 0 (lower boundary) or
1 (upper boundary). Alternatively, character values "lower" and "upper"
or logical values (FALSE/TRUE) are accepted and will be converted
automatically.}

\item{links}{A named list of link functions for the model parameters.
Available parameters depend on the version: "simple" has \code{drift}, \code{bound},
\code{ndt}, \code{s}, and \code{sndt}; "crisk" additionally has \code{zr}. Default links are
"log" for most parameters (including \code{sndt}) and "logit" for \code{zr}. While
\code{sndt} is fixed at 0 (the default), the family internally uses the
identity link for it so that the constant equals 0 on the natural scale;
the log link applies as soon as \code{sndt} is estimated, keeping all
predicted values positive.}

\item{version}{A character string specifying which version of the cswald
model to use. Options are:
\itemize{
\item \code{"simple"} (default): The standard censored shifted Wald model,
which treats error responses as censored correct responses. Best suited
for tasks with few errors (<20\%). \strong{Note:} The \code{bound} parameter in
the simple version represents the distance from the starting point to
the correct boundary, which is half the total boundary separation
in the diffusion model (assuming an unbiased starting point). To
convert to the full boundary separation (as in DDM or crisk), multiply
by 2.
\item \code{"crisk"}: The competing risks version, which models both response
types as arising from racing accumulators toward opposite boundaries.
Better suited for tasks with substantial error rates. The \code{bound}
parameter represents the total boundary separation, consistent with
the diffusion model parameterization.
}
For more details, see Miller et al. (2017).

\strong{Trial-to-trial variability in the non-decision time (\code{sndt})}

Both versions include an optional parameter \code{sndt} for uniform
trial-to-trial variability in the non-decision time: the non-decision
time is distributed as \code{Uniform(ndt, ndt + sndt)}, so \code{ndt} is the
\emph{minimum} non-decision time and the mean non-decision time is
\code{ndt + sndt/2}. It corresponds to the \code{st0} parameter in rtdists and
fast-dm. By default \code{sndt} is fixed to 0, which reproduces the standard
censored shifted Wald model. To estimate it, add a formula for \code{sndt} to
the model formula (e.g., \code{bmf(..., sndt ~ 1)}); to fix it at a different
value, supply a constant in seconds (e.g., \code{bmf(..., sndt = 0.15)}).

Without \code{sndt}, a handful of fast response times caps the entire \code{ndt}
estimate (the likelihood requires \code{ndt < min(rt)}), which biases \code{ndt},
\code{drift}, and \code{bound} when the generating process has variable
non-decision times (Miller et al., 2017, Fig. 3). Estimating \code{sndt}
removes this bias. Note that \code{sndt} itself is weakly identified at
typical trial numbers: its value lies in de-biasing the other
parameters, not in substantive interpretation. Estimating it
population-level only (\code{sndt ~ 1}, no random effects) and checking prior
sensitivity is recommended, since the prior acts as the effective
regularizer.

For the "crisk" version with \code{sndt > 0}, the two accumulators receive
independent non-decision-time draws (a race between total finishing
times). Compared to a single shared draw per trial, choice probabilities
are identical and densities differ by less than ~2\% even at \code{sndt = 0.3};
parameter recovery on data generated from this racing process is
unbiased.

\strong{Estimating \code{sndt} in the "crisk" version at substantial error rates.}
The crisk model treats the two response options as independent racing
accumulators, which only \emph{approximates} a single Wiener diffusion
process: the approximation is excellent when one boundary dominates (few
errors) but imperfect when error rates are substantial. A freed \code{sndt}
hands the model a flexible direction along which it can absorb this
approximation error rather than genuine non-decision-time variability:
in simulations with diffusion-generated data at 10-17\% errors (true
\code{bound} 1.6, \code{sndt} 0.2), the crisk fit converged to \code{bound} ~1.2 and
\code{sndt} ~0.4 even with very large samples, while the same likelihood
recovered data from its own racing process without bias. A shared
(rather than independent) non-decision-time formulation yields the same
estimates, so this is a property of the crisk approximation itself.
Options:
\itemize{
\item At low error rates (roughly below 10\%), use the \code{"simple"}
version: the censored shifted Wald is then an excellent
approximation of the diffusion process and \code{sndt} recovery is
unbiased.
\item At substantial error rates, when the data plausibly stem from a
single diffusion process, prefer the \code{ddm} model (exact two-boundary
likelihood, currently without non-decision-time variability), or
keep \code{sndt} fixed at 0 in the crisk fit -- without the extra
flexibility the crisk estimates stay close to the diffusion values.
\item Fixing \code{sndt} at a plausible nonzero value (e.g.,
\code{bmf(..., sndt = 0.1)}) bounds its influence while still allowing
the mean non-decision time to exceed the fastest response time.
\item Treat a freed \code{sndt} that comes out much larger -- together with
a much smaller \code{bound} -- than in a fit with \code{sndt} fixed (or than a
\code{ddm} fit) as the signature of approximation-error absorption, and
validate with posterior-predictive checks (\code{pp_check()}) before
interpreting the parameters.
}}

\item{...}{Additional arguments passed internally (for testing purposes).}
}
\value{
An object of class \code{bmmodel}
}
\description{
Censored-Shifted Wald Model
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks (with few errors)
\item \strong{Name:} Censored-Shifted Wald Model
\item \strong{Citation:}
\itemize{
\item Miller, R., Scherbaum, S., Heck, D. W., Goschke, T., & Enge, S. (2017).
On the Relation Between the (Censored) Shifted Wald and the Wiener Distribution as Measurement Models
for Choice Response Times. Applied Psychological Measurement, 42(2), 116-135. https://doi.org/10.1177/0146621617710465
}
\item \strong{Version:} simple
\item \strong{Requirements:}
\itemize{
\item Reaction times should be passed in seconds
}
}
\itemize{
\item The response variable should be passed numerically: 0 = lower response, 1 = upper response
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{drift}: drift rate
\item \code{bound}: boundary (distance from starting point to correct boundary)
\item \code{ndt}: non-decision time (minimum non-decision time when sndt > 0)
\item \code{s}: diffusion constant
\item \code{sndt}: range of the uniform trial-to-trial variability in the non-decision time
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
\item \code{s} = 0
\item \code{sndt} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item drift = log; bound = log; ndt = log; s = log; sndt = log
}
\item \strong{Default priors:}
\itemize{
\item \code{drift}:
\itemize{
\item \code{main}: normal(0,1)
\item \code{effects}: normal(0,0.3)
}
\item \code{bound}:
\itemize{
\item \code{main}: normal(0,0.3)
\item \code{effects}: normal(0,0.3)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-2,0.3)
\item \code{effects}: normal(0,0.3)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0,0.3)
\item \code{effects}: normal(0,0.2)
}
\item \code{sndt}:
\itemize{
\item \code{main}: normal(-2.5,1)
\item \code{effects}: normal(0,0.3)
}
}
}
}
\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# generate simulated data from the diffusion model
dat <- rcswald(n = 500, drift = 2, bound = 1.5, ndt = 0.3, zr = 0.5, s = 1)

# specify the model
model <- cswald(rt = "rt", response = "response", version = "simple")

# specify the formula
formula <- bmf(
  drift ~ 1,
  bound ~ 1,
  ndt ~ 1
)

# fit the model
fit <- bmm(
  formula = formula,
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dcswald]{dcswald()}} and \code{\link[=rcswald]{rcswald()}} for the density and random generation
functions.
}
\keyword{bmmodel}
