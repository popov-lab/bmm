% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{roc_sdt}
\alias{roc_sdt}
\title{Model-implied ROC curve from a fitted SDT model}
\usage{
roc_sdt(fit, conditions = NULL, n_points = 100, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}} from an SDT model.}

\item{conditions}{Optional data frame of predictor values for which to
compute the ROC curve. Each row defines one set of conditions. Column
names must match the predictor variables used in the formula. If \code{NULL}
(default), the function derives unique predictor combinations from the
original data.}

\item{n_points}{Integer. Number of equally-spaced points on the smooth ROC
curve for binary models (default 100). Ignored for rating models, which
always return K+1 points (K-1 threshold points plus the two extreme
endpoints (0,0) and (1,1)).}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles for the
credible band (default \code{c(0.025, 0.975)} for a 95\% CI).}

\item{...}{Additional arguments passed to \code{\link[brms:posterior_linpred.brmsfit]{brms::posterior_linpred()}}.}
}
\value{
A data frame of class \code{"bmm_sdt_roc"} with columns \code{FA}, \code{Hit},
\code{.draw}, and any condition columns from \code{conditions}. The object has a
\code{summary} attribute with columns \code{FA}, \code{Hit_mean}, \code{Hit_lower},
\code{Hit_upper} (and condition columns) summarising over posterior draws.
}
\description{
Computes the receiver operating characteristic (ROC) curve implied by the
posterior distribution of an SDT model fit with \code{\link[=bmm]{bmm()}}. For binary SDT
models the ROC is traced analytically from the posterior of \code{dprime} (and
\code{sdratio} for UV-SDT) over a grid of criterion values. For confidence rating
models the K-1 thresholds define K-1 empirical ROC points per posterior draw.
}
\details{
ROC curves are meaningful only for SDT models with an explicit response
criterion. \code{roc_sdt()} raises an error for \code{version = "mafc"} and
\code{version = "ranking"}, which lack a criterion parameter.

For Gumbel-min EV-SDT the ROC takes the power-ROC form
\eqn{Hit = FA^{\exp(-g')}}, which is available in closed form and can
be recognised as a testable signature of Gumbel-min noise.
}
\seealso{
\code{\link[=auc_sdt]{auc_sdt()}}, \code{\link[=plot.bmm_sdt_roc]{plot.bmm_sdt_roc()}}
}
