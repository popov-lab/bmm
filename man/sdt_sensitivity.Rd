% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{sdt_sensitivity}
\alias{sdt_sensitivity}
\title{Sensitivity on the noise, signal, or root-mean-square scale}
\usage{
sdt_sensitivity(
  fit,
  measure = c("da", "dn", "ds"),
  conditions = NULL,
  probs = c(0.025, 0.975),
  ...
)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}} from an SDT model.}

\item{measure}{Character vector naming the scales to return: \code{"da"}
(root-mean-square SD, the estimated parameter), \code{"dn"} (noise SD), and/or
\code{"ds"} (signal SD). Defaults to all three.}

\item{conditions}{Optional data frame of predictor values for which to
compute the ROC curve. Column names must match predictor variables used in
the formula. If \code{NULL} (default), unique predictor combinations are derived
from the data.}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles for the
credible interval (default \code{c(0.025, 0.975)}).}

\item{...}{Additional arguments passed to \code{\link[brms:posterior_linpred]{brms::posterior_linpred()}}.}
}
\value{
A data frame of class \code{"bmm_sdt_sensitivity"} with columns \code{measure},
\code{value}, \code{.draw}, and any condition columns. The object carries a \code{summary}
attribute (\code{measure}, \code{mean}, \code{lower}, \code{upper}, plus condition columns).
}
\description{
Re-expresses the posterior sensitivity of a fitted SDT model against a
different reference standard deviation. bmm estimates \eqn{d_a}, which
measures the separation of the two evidence distributions in units of their
root-mean-square SD. Under unequal variance the same separation can also be
read against the noise SD (\eqn{d_N}, the classical \eqn{d'}) or against the
signal SD (\eqn{d_S}). This function returns any of the three as posterior
draws, so contrasts and intervals can be computed on whichever scale a
literature reports.
}
\details{
The model places the noise distribution at \eqn{-\delta/2} with SD
\eqn{\sigma_N = 1} and the signal distribution at \eqn{+\delta/2} with SD
\eqn{\sigma_S = \exp(\mathrm{sdratio})}, so \eqn{\delta} is the separation in
noise-SD units. Dividing that separation by each reference SD gives

\deqn{d_N = \delta / \sigma_N, \quad d_S = \delta / \sigma_S, \quad
      d_a = \delta / \sqrt{(\sigma_N^2 + \sigma_S^2)/2}.}

The estimated parameter is \eqn{d_a}, hence
\eqn{\delta = d_a \sqrt{(1 + \sigma_S^2)/2}} and

\deqn{d_N = d_a \sqrt{(1 + \sigma_S^2)/2}, \qquad
      d_S = d_a \sqrt{(1 + \sigma_S^2)/2} \,/\, \sigma_S.}

All three coincide when \code{sdratio} is 0 (equal variance), which is the case
for \code{\link[=sdt_mafc]{sdt_mafc()}} and for any fit that does not give \code{sdratio} a formula. The
conversion is applied draw by draw, so the returned intervals propagate the
joint posterior uncertainty in \code{d} and \code{sdratio} rather than combining
point estimates.

Only \eqn{d_a} is invariant to which distribution is treated as the
reference. \eqn{d_N} and \eqn{d_S} are not comparable across conditions that
differ in \code{sdratio}: two conditions with identical discriminability can show
a large, confidently estimated \eqn{d_N} difference. Prefer \eqn{d_a} for
contrasts, and use \eqn{d_N}/\eqn{d_S} for comparison with published values.
}
\seealso{
\code{\link[=auc_sdt]{auc_sdt()}}, \code{\link[=roc_sdt]{roc_sdt()}}, \code{\link[=latent_sdt]{latent_sdt()}}
}
