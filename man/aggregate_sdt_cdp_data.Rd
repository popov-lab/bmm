% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_cdp.R
\name{aggregate_sdt_cdp_data}
\alias{aggregate_sdt_cdp_data}
\title{Aggregate long-format Remember/Know data for \code{\link[=sdt_cdp]{sdt_cdp()}}}
\usage{
aggregate_sdt_cdp_data(data, judgment, confidence, count = NULL, response = "")
}
\arguments{
\item{data}{A data frame in long format. Cells are defined by all columns
other than \code{judgment}, \code{confidence}, and \code{count} (e.g. participant,
stimulus class, and any condition variables), which are carried over to
the output unchanged.}

\item{judgment}{The name of the column coding the memory judgment, with
values \code{"new"}, \code{"remember"}, \code{"know"}, and optionally \code{"guess"}.}

\item{confidence}{The name of the column coding confidence on the unified
old/new scale (integer, 1 = most confident "new" to K = most confident
"old"; "new" judgments occupy the low levels, "old" judgments the high
levels).}

\item{count}{Optional name of a column of response counts. If \code{NULL}
(default), each row is treated as a single trial.}

\item{response}{Optional common prefix for the generated count columns
(default \code{""}). Pass the same value to the \code{response} argument of
\code{\link[=sdt_cdp]{sdt_cdp()}}.}
}
\value{
A data frame with one row per cell: the cell-defining columns
followed by the response count columns, ready to pass to \code{\link[=bmm]{bmm()}} with an
\code{\link[=sdt_cdp]{sdt_cdp()}} model. The numbers of "new" and "old" confidence levels are
inferred from the data.
}
\description{
Reshapes long-format Remember/Know recognition data -- one row
per trial, or one row per response category with a count column -- into
the aggregated wide format \code{\link[=sdt_cdp]{sdt_cdp()}} is fit to: one row per cell and
one count column per response category, named \verb{new1 ... new<n_new>},
\verb{know<k>} / \verb{remember<k>} (and \verb{guess<k>} when the data contain "guess"
judgments), with \code{k} on the unified confidence scale
\verb{n_new + 1 ... n_new + n_old}.
}
\examples{
dat <- data.frame(
  stimulus = c(1L, 1L, 1L, 0L, 0L, 0L),
  judgment = c("remember", "know", "new", "new", "know", "new"),
  confidence = c(3L, 2L, 1L, 1L, 3L, 1L),
  count = c(40L, 25L, 15L, 55L, 10L, 20L)
)
aggregate_sdt_cdp_data(dat, "judgment", "confidence", "count")
}
\keyword{transform}
