% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_utility.R
\name{utility}
\alias{utility}
\title{Random-Utility Choice Model}
\usage{
utility(
  resp_cats,
  num_options = NULL,
  payoffs = NULL,
  value_cols = NULL,
  utility_fn = c("linear", "power"),
  weighting = c("none", "prelec"),
  choice_rule = c("softmax", "simple"),
  numeraire = NULL,
  ...
)
}
\arguments{
\item{resp_cats}{Character vector of the data columns holding the response
counts for each choice category.}

\item{num_options}{Either a single number / integer vector giving the number
of candidates per category (constant across trials), or the names of the
data columns holding per-trial candidate counts. Defaults to one candidate
per category.}

\item{payoffs}{A numeric matrix (constant payoffs) or a data.frame whose cells
are numbers (constant) or the names of per-trial data columns. Rows are named
by \code{resp_cats}, columns by the weight parameters, and must include the
numeraire column (default \code{"b"}). Generates the per-category activation
formulas. Mutually exclusive with \code{value_cols}.}

\item{value_cols}{A named character vector mapping response categories to
per-trial value columns, e.g. \code{c(corr = "V")}. Mutually exclusive with
\code{payoffs}.}

\item{utility_fn}{Value function for the \code{value_cols} path: \code{"linear"}
(\code{gamma * V}) or \code{"power"} (\code{gamma * V^rho}).}

\item{weighting}{Probability weighting for the \code{value_cols} path: \code{"none"} or
\code{"prelec"} (adds \code{alpha}; requires \code{choice_rule = "softmax"}).}

\item{choice_rule}{\code{"softmax"} (random-utility logit) or \code{"simple"} (Luce ratio
over raw utilities).}

\item{numeraire}{Fixes the scaling weight: \code{"b"} (fixed to 1), \code{c(b = value)},
or omitted for the path default.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
A random-utility choice model for multinomial (category-count) data. Each
response category carries a systematic utility built from either a known
\code{payoffs} matrix (welfare-weight designs, e.g. economic games) or per-trial
\code{value_cols} (expected-utility designs), and the category is chosen through a
softmax (multinomial-logit / random-utility) or simple (Luce ratio) rule.

This model covers \strong{category-count choice} — you observe how often each of a
fixed set of categories was chosen. It does \strong{not} cover per-option
attribute / binary-lottery choice (prospect theory a la Nilsson et al., 2011);
that needs a different data interface and is out of scope.
}
\details{
\itemize{
\item \strong{Domain:} Value-based / economic decision making
\item \strong{Task:} n-alternative categorical choice
\item \strong{Name:} Random-Utility Choice Model
\item \strong{Citation:}
\itemize{
\item Gross, J., Gotz, F., Reher, F., & Toscano, H. (2025). Nested social dilemmas. Communications Psychology, 3. McFadden, D. (1974). Conditional logit analysis of qualitative choice behavior.
}
}

Built on the same multinomial-logit likelihood as the \code{\link[=m3]{m3()}} model.
}
\examples{
\dontshow{if (isTRUE(Sys.getenv("BMM_EXAMPLES"))) withAutoprint(\{ # examplesIf}
# Welfare weights from a known payoff matrix (economic game)
model <- utility(
  resp_cats = c("nkeep", "ningroup", "nuniversal"),
  payoffs = rbind(
    nkeep      = c(b = 1.0, wi = 0.0, wo = 0.0),
    ningroup   = c(b = 0.5, wi = 1.0, wo = 0.0),
    nuniversal = c(b = 0.3, wi = 0.6, wo = 0.9)
  ),
  choice_rule = "simple",
  numeraire = c(b = 1)
)

fit <- bmm(bmf(wi ~ 1, wo ~ 1), data = my_data, model = model)
\dontshow{\}) # examplesIf}
}
\keyword{bmmodel}
