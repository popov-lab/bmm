% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{lba_dist}
\alias{lba_dist}
\alias{dlba}
\alias{rlba}
\alias{plba}
\alias{qlba}
\title{LBA Distribution}
\usage{
dlba(
  rt,
  response,
  drift,
  B,
  A,
  ndt,
  s = 1,
  distribution = c("normal", "gamma", "frechet", "lognormal"),
  log = FALSE
)

rlba(
  n,
  drift,
  B,
  A,
  ndt,
  s = 1,
  distribution = c("normal", "gamma", "frechet", "lognormal")
)

plba(
  q,
  drift,
  B,
  A,
  ndt,
  s = 1,
  distribution = c("normal", "gamma", "frechet", "lognormal"),
  lower.tail = TRUE,
  log.p = FALSE
)

qlba(
  p,
  drift,
  B,
  A,
  ndt,
  s = 1,
  distribution = c("normal", "gamma", "frechet", "lognormal"),
  lower.tail = TRUE,
  log.p = FALSE
)
}
\arguments{
\item{rt}{Numeric vector of response times in seconds.}

\item{response}{Integer vector indicating the winning accumulator
(1, 2, ..., K).}

\item{drift}{Numeric vector of drift rate parameters (one per accumulator).
Interpretation depends on \code{distribution}: mean drift for \code{"normal"}, shape
for \code{"gamma"} and \code{"frechet"}, meanlog for \code{"lognormal"}.}

\item{B}{Numeric. Threshold gap (> 0). The effective threshold is
\code{b = A + B}, guaranteeing \code{b > A}.}

\item{A}{Numeric. Maximum starting point for the uniform starting point
distribution on \verb{[0, A]}. Must be >= 0.}

\item{ndt}{Numeric. Non-decision time in seconds (>= 0).}

\item{s}{Numeric. Scale parameter (> 0, default = 1). Interpretation depends
on \code{distribution}: drift SD for \code{"normal"}, rate for \code{"gamma"}, scale for
\code{"frechet"}, sdlog for \code{"lognormal"}. Typically fixed to 1 for
identifiability.}

\item{distribution}{Character. The drift rate distribution. One of
\code{"normal"} (default), \code{"gamma"}, \code{"frechet"}, or \code{"lognormal"}.}

\item{log}{Logical. If \code{TRUE}, return log-densities. Default \code{FALSE}.}

\item{n}{Integer. Number of samples to generate.}

\item{q}{Numeric vector of quantiles (response times).}

\item{lower.tail}{Logical. If \code{TRUE} (default), probabilities are
\code{P(RT <= q)}.}

\item{log.p}{Logical. If \code{TRUE}, probabilities are on the log scale.}

\item{p}{Numeric vector of probabilities.}
}
\value{
\itemize{
\item \code{dlba()} returns a numeric vector of (log-)densities.
\item \code{rlba()} returns a data.frame with columns \code{rt} and \code{response}.
\item \code{plba()} returns a numeric vector of (log-)probabilities.
\item \code{qlba()} returns a numeric vector of quantiles (response times).
}
}
\description{
Density, distribution function, quantile function, and random
generation for the Linear Ballistic Accumulator (LBA) model with multiple
drift rate distributions.
}
\examples{
dat <- rlba(n = 1000, drift = c(3, 1.5), B = 0.5, A = 0.5, ndt = 0.3)
head(dat)
dlba(dat$rt[1:5], dat$response[1:5], drift = c(3, 1.5),
     B = 0.5, A = 0.5, ndt = 0.3)

}
\references{
Brown, S. D., & Heathcote, A. (2008). The simplest complete
model of choice response time: Linear ballistic accumulation. \emph{Cognitive
Psychology}, 57(3), 153-178.
}
