% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sdt_binary.R
\name{sdt_binary}
\alias{sdt_binary}
\title{Binary Signal Detection Theory Model}
\usage{
sdt_binary(
  response,
  stimulus,
  n_trials,
  dist = c("normal", "logistic", "gumbel_min", "gumbel_max"),
  links = NULL,
  ...
)
}
\arguments{
\item{response}{A single string naming the column with "old"/"signal"
response counts.}

\item{stimulus}{The name of the variable coding the stimulus type.
Must be coded as 0 (noise/new) and 1 (signal/old).}

\item{n_trials}{The name of the variable containing the total number of
trials per cell.}

\item{dist}{Character. The noise distribution to use:
\itemize{
\item "normal" (default): Gaussian SDT
\item "logistic": Logistic SDT
\item "gumbel_min": Extreme-value (Gumbel minimum) SDT
\item "gumbel_max": Gumbel maximum SDT
}}

\item{links}{A named list of link functions for the parameters.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
Binary Signal Detection Theory Model
}
\details{
\itemize{
\item \strong{Domain:} Perception & Recognition Memory
\item \strong{Task:} Signal/Noise or Old/New Recognition
\item \strong{Name:} Signal Detection Theory (Binary)
\item \strong{Citation:}
\itemize{
\item Green, D. M., & Swets, J. A. (1966). Signal detection theory and psychophysics. Wiley.
}
\item \strong{Version:} binary
\item \strong{Requirements:}

Provide pre-aggregated data with the following columns:
}
\itemize{
\item Response counts (n_old): number of 'old'/'signal' responses
\item Stimulus type (stimulus): 0 = noise, 1 = signal
\item Number of trials (n_trials): total trials per cell
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{mu}: Internal parameter (fixed to 0)
\item \code{dprime}: Sensitivity: distance between signal and noise distributions
\item \code{criterion}: Response bias: location of decision boundary
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{mu} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item mu = identity; dprime = identity; criterion = identity
}
\item \strong{Default priors:}
\itemize{
\item \code{dprime}:
\itemize{
\item \code{main}: normal(1, 1)
\item \code{effects}: normal(0, 0.5)
}
\item \code{criterion}:
\itemize{
\item \code{main}: normal(0, 1.5)
\item \code{effects}: normal(0, 0.5)
}
}
}
}
\examples{
\dontrun{
dat <- rsdt_binary(n_per_cell = 100, n_subjects = 20,
                   dprime = 1.5, criterion = 0.2)

model <- sdt_binary(
  response = "n_old",
  stimulus = "stimulus",
  n_trials = "n_trials"
)

fit <- bmm(
  formula = bmf(dprime ~ 1, criterion ~ 1),
  data = dat,
  model = model,
  cores = 4,
  backend = "cmdstanr"
)
}
}
\references{
Green, D. M., & Swets, J. A. (1966). \emph{Signal detection theory and
psychophysics}. Wiley.

DeCarlo, L. T. (1998). Signal detection theory and generalized linear
models. \emph{Psychological Methods}, \emph{3}(2), 186--205.
\doi{10.1037/1082-989X.3.2.186}
}
\keyword{bmmodel}
