% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-analysis.R
\name{auc_sdt}
\alias{auc_sdt}
\title{Area under the ROC curve from a fitted SDT model}
\usage{
auc_sdt(fit, conditions = NULL, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{fit}{A \code{bmmfit} object returned by \code{\link[=bmm]{bmm()}} from an SDT model.}

\item{conditions}{Optional data frame of predictor values (see \code{\link[=roc_sdt]{roc_sdt()}}).}

\item{probs}{Numeric vector of length 2. Quantiles for the credible interval
(default \code{c(0.025, 0.975)}).}

\item{...}{Additional arguments passed to \code{\link[brms:posterior_linpred]{brms::posterior_linpred()}} (for
analytical cases) or \code{\link[=roc_sdt]{roc_sdt()}} (for numerical cases).}
}
\value{
A data frame of class \code{"bmm_sdt_auc"} with columns \code{AUC}, \code{.draw},
and any condition columns. The object has a \code{summary} attribute with
columns \code{AUC_mean}, \code{AUC_lower}, \code{AUC_upper} (and condition columns).
}
\description{
Computes the area under the ROC curve (AUC) from the posterior distribution
of an SDT model. For Gaussian and Gumbel-min EV-SDT the AUC is computed
analytically from \code{dprime} draws; for all other configurations it is
computed via trapezoidal integration on the ROC points from \code{\link[=roc_sdt]{roc_sdt()}}.
}
\details{
Analytical formulas:
\itemize{
\item Normal EV-SDT: \eqn{AUC = \Phi(d'/\sqrt{2})}
\item Gumbel-min EV-SDT: \eqn{AUC = \text{logistic}(g') = 1/(1+\exp(-g'))}
}
}
\seealso{
\code{\link[=roc_sdt]{roc_sdt()}}, \code{\link[=plot.bmm_sdt_auc]{plot.bmm_sdt_auc()}}
}
