% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{sdt_dp_dist}
\alias{sdt_dp_dist}
\alias{dsdt_dp}
\alias{rsdt_dp}
\title{Distribution functions for Dual Process SDT (DPSDT)}
\usage{
dsdt_dp(
  counts,
  stimulus,
  dprime,
  thresholds,
  Ro = NULL,
  Rn = NULL,
  sdratio = 1,
  dist = "normal",
  log = FALSE
)

rsdt_dp(
  n_per_cell,
  n_subjects,
  dprime,
  criterion,
  Ro = NULL,
  Rn = NULL,
  sdratio = 1,
  dist = "normal",
  n_ratings = NULL,
  spacing = NULL,
  deltas = NULL,
  threshold_type = "parsimonious"
)
}
\arguments{
\item{counts}{Integer vector of length K. Response counts per rating
category for a single observation.}

\item{stimulus}{Integer (0 or 1). Stimulus type: 0 = noise, 1 = signal.}

\item{dprime}{Numeric. Sensitivity (familiarity) parameter.}

\item{thresholds}{Numeric vector of length K-1. Ordered decision thresholds.}

\item{Ro}{Numeric. Recollection probability for old items (0 to 1). Adds
mass to the highest confidence "old" category.}

\item{Rn}{Numeric. Recollection probability for new items (0 to 1). Adds
mass to the highest confidence "new" category.}

\item{sdratio}{Numeric. Ratio of signal to noise standard deviations
(default 1).}

\item{dist}{Character. Noise distribution: "normal" (default), "logistic",
"gumbel_min", or "gumbel_max".}

\item{log}{Logical. If \code{TRUE}, returns log-density (default \code{FALSE}).}

\item{n_per_cell}{Integer. Number of trials per stimulus type per subject.}

\item{n_subjects}{Integer. Number of subjects.}

\item{criterion}{Numeric. Central decision threshold.}

\item{n_ratings}{Integer. Number of response categories (must be >= 3).}

\item{spacing}{Numeric. Threshold spacing parameter.}

\item{deltas}{Numeric vector. Parameters for "log_distance" or "log_ratio"
threshold types.}

\item{threshold_type}{Character. Threshold parameterization: "parsimonious"
(default), "equidistant", "log_distance", or "log_ratio".}
}
\value{
\code{dsdt_dp} returns the (log-)density. \code{rsdt_dp} returns a data frame
with columns \code{id}, \code{stimulus}, \code{r1}, ..., \code{rK}, and \code{nTrials}.
}
\description{
Density and random generation for dual process signal detection
theory (Yonelinas, 1994). Extends rating SDT with recollection parameters
\code{Ro} (old items) and \code{Rn} (new items) that add probability mass to the
extreme confidence categories.
}
\examples{
# DPSDT density (K=4)
dsdt_dp(counts = c(5, 15, 25, 55), stimulus = 1,
        dprime = 1.0, thresholds = c(-0.5, 0.0, 0.5),
        Ro = 0.3, Rn = 0.1)
# Generate DPSDT data
dat <- rsdt_dp(n_per_cell = 100, n_subjects = 10,
               dprime = 1.0, criterion = 0,
               Ro = 0.3, Rn = 0.1,
               n_ratings = 4, spacing = 0.5)
}
\references{
Yonelinas, A. P. (1994). Receiver-operating characteristics in recognition
memory: Evidence for a dual-process model. \emph{Journal of Experimental
Psychology: Learning, Memory, and Cognition}, \emph{20}(6), 1341--1354.
\doi{10.1037/0278-7393.20.6.1341}
}
\keyword{distribution}
