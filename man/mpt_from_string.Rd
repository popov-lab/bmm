% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mpt_import.R
\name{mpt_from_string}
\alias{mpt_from_string}
\title{Create an MPT model from an MPTinR-style model string}
\usage{
mpt_from_string(
  text,
  tree_names,
  categories = NULL,
  tree_id = NULL,
  covariates = NULL,
  simplex = NULL,
  links = "logit"
)
}
\arguments{
\item{text}{Character. The model definition, either as a single string or
as a vector of lines.}

\item{tree_names}{Character vector with one name per tree block, in the
order the blocks appear. For multi-tree models the names must match the
values of the tree identifier column in the data.}

\item{categories}{Character vector with the response category of each line
within a tree, used for lines without an inline \verb{# category} comment.
Must have as many entries as the tree with the most lines.}

\item{tree_id}{Character. Name of the data column whose values name the tree
each observation belongs to (see \code{\link[=mpt]{mpt()}}).}

\item{covariates, simplex, links}{passed to \code{\link[=mpt]{mpt()}}.}
}
\value{
An object of class \code{bmmodel} (see \code{\link[=mpt]{mpt()}})
}
\description{
Parses a model definition in the format used by MPTinR model
files and returns the corresponding \code{\link[=mpt]{mpt()}} model object. Each line is
one branch probability expression; blank lines separate trees. The
response category of each line is given either by an inline comment
(\code{expression # category}) or by the \code{categories} argument. Lines that
terminate in the same response category are summed.
}
\examples{
model_2htm <- "
D + (1 - D) * g        # old
(1 - D) * (1 - g)      # new

(1 - D) * g            # old
D + (1 - D) * (1 - g)  # new
"
model <- mpt_from_string(
  model_2htm,
  tree_names = c("old", "new"),
  tree_id = "item_type"
)
model
}
\keyword{transform}
